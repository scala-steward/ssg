/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for const.
 * Ported from: terser/test/compress/const.js (5 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressConstSuite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // issue_1191
  // =========================================================================
  test("issue_1191".fail) {
    assertCompresses(
      input = """function foo(rot) {
            const rotTol = 5;
            if (rot < -rotTol || rot > rotTol)
                bar();
            baz();
        }""".stripMargin.trim,
      expected = """function foo(rot) {
            (rot < -5 || rot > 5) && bar();
            baz();
        }""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = false,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        reduceFuncs = true,
        reduceVars = true,
        sequencesLimit = 0,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_1194
  // =========================================================================
  test("issue_1194".fail) {
    assertCompresses(
      input = """function f1() {const a = "X"; return a + a;}
        function f2() {const aa = "X"; return aa + aa;}
        function f3() {const aaa = "X"; return aaa + aaa;}""".stripMargin.trim,
      expected = """function f1(){return"XX"}
        function f2(){return"XX"}
        function f3(){return"XX"}""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = false,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        reduceFuncs = true,
        reduceVars = true,
        sequencesLimit = 0,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_1396
  // =========================================================================
  test("issue_1396".fail) {
    assertCompresses(
      input = """function foo(a) {
            const VALUE = 1;
            console.log(2 | VALUE);
            console.log(VALUE + 1);
            console.log(VALUE);
            console.log(a & VALUE);
        }
        function bar() {
            const s = "01234567890123456789";
            console.log(s + s + s + s + s);

            const CONSTANT = "abc";
            console.log(CONSTANT + CONSTANT + CONSTANT + CONSTANT + CONSTANT);
        }""".stripMargin.trim,
      expected = """function foo(a) {
            console.log(3);
            console.log(2);
            console.log(1);
            console.log(a & 1);
        }
        function bar() {
            const s = "01234567890123456789";
            console.log(s + s + s + s + s);

            console.log("abcabcabcabcabc");
        }""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = false,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        reduceFuncs = true,
        reduceVars = true,
        sequencesLimit = 0,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // unused_regexp_literal
  // =========================================================================
  test("unused_regexp_literal".fail) {
    assertCompresses(
      input = "function f(){ var a = /b/; }",
      expected = "function f(){}",
      options = AllOff.copy(
        booleans = true,
        collapseVars = false,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        sequencesLimit = 0,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // regexp_literal_not_const
  // =========================================================================
  test("regexp_literal_not_const".fail) {
    assertCompresses(
      input = """(function(){
            var result;
            const s = 'acdabcdeabbb';
            const REGEXP_LITERAL = /ab*/g;
            while (result = REGEXP_LITERAL.exec(s)) {
                console.log(result[0]);
            }
        })()""".stripMargin.trim,
      expected = """(function() {
            var result;
            const REGEXP_LITERAL = /ab*/g;
            while (result = REGEXP_LITERAL.exec("acdabcdeabbb")) console.log(result[0]);
        })()""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = false,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        reduceFuncs = true,
        reduceVars = true,
        sequencesLimit = 0,
        sideEffects = true,
        unused = true
      )
    )
  }

}
