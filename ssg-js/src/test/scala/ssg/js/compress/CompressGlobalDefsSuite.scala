/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for global_defs.
 * Ported from: terser/test/compress/global_defs.js (10 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }
import ssg.js.compress.InlineLevel

final class CompressGlobalDefsSuite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // must_replace
  // =========================================================================
  test("must_replace".fail) {
    assertCompresses(
      input = "console.log(D)",
      expected = "console.log(\"foo bar\")",
      options = AllOff
    )
  }

  // =========================================================================
  // keyword
  // =========================================================================
  test("keyword".fail) {
    assertCompresses(
      input = "console.log(undefined, NaN, Infinity)",
      expected = "console.log(0, 1, 2)",
      options = AllOff
    )
  }

  // =========================================================================
  // object
  // =========================================================================
  test("object".fail) {
    assertCompresses(
      input = """function f(CONFIG) {
            // CONFIG not global - do not replace
            return CONFIG.VALUE;
        }
        function g() {
            var CONFIG = { VALUE: 1 };
            // CONFIG not global - do not replace
            return CONFIG.VALUE;
        }
        function h() {
            return CONFIG.VALUE;
        }
        if (CONFIG.DEBUG[0])
            console.debug("foo")""".stripMargin.trim,
      expected = """function f(CONFIG) {
            return CONFIG.VALUE;
        }
        function g() {
            var CONFIG = { VALUE: 1 };
            return CONFIG.VALUE;
        }
        function h() {
            return 42;
        }
        if (0)
            console.debug("foo")""".stripMargin.trim,
      options = AllOff.copy(
        evaluate = true,
        sideEffects = true,
        unsafe = true
      )
    )
  }

  // =========================================================================
  // expanded
  // =========================================================================
  test("expanded".fail) {
    assertCompresses(
      input = """function f(CONFIG) {
            // CONFIG not global - do not replace
            return CONFIG.VALUE;
        }
        function g() {
            var CONFIG = { VALUE: 1 };
            // CONFIG not global - do not replace
            return CONFIG.VALUE;
        }
        function h() {
            return CONFIG.VALUE;
        }
        if (CONFIG.DEBUG[0])
            console.debug("foo")""".stripMargin.trim,
      expected = """function f(CONFIG) {
            return CONFIG.VALUE;
        }
        function g() {
            var CONFIG = { VALUE: 1 };
            return CONFIG.VALUE;
        }
        function h() {
            return 42;
        }
        if ([0][0])
            console.debug("foo")""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // mixed
  // =========================================================================
  test("mixed".fail) {
    assertCompresses(
      input = """const FOO = { BAR: 0 };
        console.log(FOO.BAR);
        console.log(++CONFIG.DEBUG);
        console.log(++CONFIG.VALUE);
        console.log(++CONFIG["VAL" + "UE"]);
        console.log(++DEBUG[CONFIG.VALUE]);
        CONFIG.VALUE.FOO = "bar";
        console.log(CONFIG)""".stripMargin.trim,
      expected = """const FOO = { BAR: 0 };
        console.log("moo");
        console.log(++CONFIG.DEBUG);
        console.log(++CONFIG.VALUE);
        console.log(++CONFIG.VALUE);
        console.log(++DEBUG[42]);
        CONFIG.VALUE.FOO = "bar";
        console.log(CONFIG)""".stripMargin.trim,
      options = AllOff.copy(
        evaluate = true,
        properties = true
      )
    )
  }

  // =========================================================================
  // issue_1801
  // =========================================================================
  test("issue_1801".fail) {
    assertCompresses(
      input = "console.log(CONFIG.FOO.BAR)",
      expected = "console.log(!0)",
      options = AllOff.copy(
        booleans = true
      )
    )
  }

  // =========================================================================
  // issue_1986
  // =========================================================================
  test("issue_1986".fail) {
    assertCompresses(
      input = "alert(42)",
      expected = "console.log(42)",
      options = AllOff
    )
  }

  // =========================================================================
  // issue_2167
  // =========================================================================
  test("issue_2167".fail) {
    assertCompresses(
      input = """if (isDevMode()) {
            greetOverlord();
        }
        doWork()""".stripMargin.trim,
      expected = "doWork()",
      options = AllOff.copy(
        conditionals = true,
        deadCode = true,
        evaluate = true,
        passes = 2,
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // issue_3217
  // =========================================================================
  test("issue_3217".fail) {
    assertCompresses(
      input = "o.fn()",
      expected = "console.log(42)",
      options = AllOff.copy(
        collapseVars = true,
        inline = InlineLevel.InlineFull,
        properties = true,
        reduceVars = true,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // conditional_chains
  // =========================================================================
  test("conditional_chains".fail) {
    assertCompresses(
      input = "console.log(a?.b.c)",
      expected = "console.log(\"d\")",
      options = AllOff
    )
  }

}
