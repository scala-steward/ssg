/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-1003.
 * Ported from: terser/test/compress/issue-1003.js (1 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue1003Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // nullish_coalescing_boolean_expr
  // =========================================================================
  test("nullish_coalescing_boolean_expr") {
    assertCompresses(
      input = """(function(a, b) {
            if(!(a ?? b)) throw new Error("Some error");
        })()""".stripMargin.trim,
      expected = """(function(a, b) {
            if(!(a ?? b)) throw new Error("Some error");
        })()""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        ecma = 2015
      )
    )
  }

}
