/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue_1014.
 * Ported from: terser/test/compress/issue_1014.js (4 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue1014Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // ternary_and_private_fields
  // =========================================================================
  test("ternary_and_private_fields".fail) {
    assertCompresses(
      input = """class A {
            #fail = false;
            #pass = "PASS";
            print() {
                console.log(this.#fail ? this.#fail : this.#pass);
            }
        }
        new A().print()""".stripMargin.trim,
      expected = """class A {
            #fail = false;
            #pass = "PASS";
            print() {
                console.log(this.#fail ? this.#fail : this.#pass);
            }
        }
        new A().print()""".stripMargin.trim,
      options = AllOff.copy(
        conditionals = true
      )
    )
  }

  // =========================================================================
  // ternary_and_private_public_fields
  // =========================================================================
  test("ternary_and_private_public_fields".fail) {
    assertCompresses(
      input = """class A {
            fail = false;
            #pass = "PASS";
            print() {
                console.log(this.fail ? this.fail : this.#pass);
            }
        }
        new A().print()""".stripMargin.trim,
      expected = """class A {
            fail = false;
            #pass = "PASS";
            print() {
                console.log(this.fail ? this.fail : this.#pass);
            }
        }
        new A().print()""".stripMargin.trim,
      options = AllOff.copy(
        conditionals = true
      )
    )
  }

  // =========================================================================
  // ternary_and_private_methods
  // =========================================================================
  test("ternary_and_private_methods".fail) {
    assertCompresses(
      input = """class A {
            #fail() { return false; }
            get #pass() { return "PASS"; }
            print() {
                console.log(this.#fail() ? this.#fail() : this.#pass);
            }
        }
        new A().print()""".stripMargin.trim,
      expected = """class A {
            #fail() { return false; }
            get #pass() { return "PASS"; }
            print() {
                console.log(this.#fail() ? this.#fail() : this.#pass);
            }
        }
        new A().print()""".stripMargin.trim,
      options = AllOff.copy(
        conditionals = true
      )
    )
  }

  // =========================================================================
  // ternary_and_private_static_fields
  // =========================================================================
  test("ternary_and_private_static_fields".fail) {
    assertCompresses(
      input = """class A {
            static #fail = false;
            static #pass = "PASS";
            print() {
                console.log(A.#fail ? A.#fail : A.#pass);
            }
        }
        new A().print()""".stripMargin.trim,
      expected = """class A {
            static #fail = false;
            static #pass = "PASS";
            print() {
                console.log(A.#fail ? A.#fail : A.#pass);
            }
        }
        new A().print()""".stripMargin.trim,
      options = AllOff.copy(
        conditionals = true
      )
    )
  }

}
