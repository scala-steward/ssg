/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-1034.
 * Ported from: terser/test/compress/issue-1034.js (6 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue1034Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // non_hoisted_function_after_return
  // =========================================================================
  test("non_hoisted_function_after_return".fail) {
    assertCompresses(
      input = """function foo(x) {
            if (x) {
                return bar();
                not_called1();
            } else {
                return baz();
                not_called2();
            }
            function bar() { return 7; }
            return not_reached;
            function UnusedFunction() {}
            function baz() { return 8; }
        }""".stripMargin.trim,
      expected = """function foo(x) {
            return x ? bar() : baz();
            function bar() { return 7 }
            function baz() { return 8 }
        }""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = false,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // non_hoisted_function_after_return_2a
  // =========================================================================
  test("non_hoisted_function_after_return_2a".fail) {
    assertCompresses(
      input = """function foo(x) {
            if (x) {
                return bar(1);
                var a = not_called(1);
            } else {
                return bar(2);
                var b = not_called(2);
            }
            var c = bar(3);
            function bar(x) { return 7 - x; }
            function nope() {}
            return b || c;
        }""".stripMargin.trim,
      expected = """function foo(x) {
            return bar(x ? 1 : 2);
            function bar(x) {
                return 7 - x;
            }
        }""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = false,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = false,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        passes = 2,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // non_hoisted_function_after_return_2b
  // =========================================================================
  test("non_hoisted_function_after_return_2b".fail) {
    assertCompresses(
      input = """function foo(x) {
            if (x) {
                return bar(1);
            } else {
                return bar(2);
                var b;
            }
            var c = bar(3);
            function bar(x) {
                return 7 - x;
            }
            return b || c;
        }""".stripMargin.trim,
      expected = """function foo(x) {
            return bar(x ? 1 : 2);
            function bar(x) { return 7 - x; }
        }""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = false,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = false,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // non_hoisted_function_after_return_strict
  // =========================================================================
  test("non_hoisted_function_after_return_strict".fail) {
    assertCompresses(
      input = """"use strict";
        function foo(x) {
            if (x) {
                return bar();
                not_called1();
            } else {
                return baz();
                not_called2();
            }
            function bar() { return 7; }
            return not_reached;
            function UnusedFunction() {}
            function baz() { return 8; }
        }
        console.log(foo(0), foo(1))""".stripMargin.trim,
      expected = """"use strict";
        function foo(x) {
            return x ? bar() : baz();
            function bar() { return 7 }
            function baz() { return 8 }
        }
        console.log(foo(0), foo(1))""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = false,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // non_hoisted_function_after_return_2a_strict
  // =========================================================================
  test("non_hoisted_function_after_return_2a_strict".fail) {
    assertCompresses(
      input = """"use strict";
        function foo(x) {
            if (x) {
                return bar(1);
                var a = not_called(1);
            } else {
                return bar(2);
                var b = not_called(2);
            }
            var c = bar(3);
            function bar(x) { return 7 - x; }
            function nope() {}
            return b || c;
        }
        console.log(foo(0), foo(1))""".stripMargin.trim,
      expected = """"use strict";
        function foo(x) {
            return bar(x ? 1 : 2);
            function bar(x) {
                return 7 - x;
            }
        }
        console.log(foo(0), foo(1))""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = false,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = false,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        passes = 2,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // non_hoisted_function_after_return_2b_strict
  // =========================================================================
  test("non_hoisted_function_after_return_2b_strict".fail) {
    assertCompresses(
      input = """"use strict";
        function foo(x) {
            if (x) {
                return bar(1);
            } else {
                return bar(2);
                var b;
            }
            var c = bar(3);
            function bar(x) {
                return 7 - x;
            }
            return b || c;
        }
        console.log(foo(0), foo(1))""".stripMargin.trim,
      expected = """"use strict";
        function foo(x) {
            return bar(x ? 1 : 2);
            function bar(x) { return 7 - x; }
        }
        console.log(foo(0), foo(1))""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = false,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = false,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        sideEffects = true,
        unused = true
      )
    )
  }

}
