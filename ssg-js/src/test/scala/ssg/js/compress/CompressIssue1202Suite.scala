/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-1202.
 * Ported from: terser/test/compress/issue-1202.js (2 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue1202Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // mangle_keep_fnames_false
  // =========================================================================
  test("mangle_keep_fnames_false".fail) {
    assertCompresses(
      input = """"use strict";
        function total() {
            return function n(a, b, c) {
                return a + b + c;
            };
        }""".stripMargin.trim,
      expected = """"use strict";
        function total() {
            return function t(n, r, u) {
                return n + r + u;
            };
        }""".stripMargin.trim,
      options = AllOff.copy(
        keepFargs = true,
        keepFnames = true
      )
    )
  }

  // =========================================================================
  // mangle_keep_fnames_true
  // =========================================================================
  test("mangle_keep_fnames_true".fail) {
    assertCompresses(
      input = """"use strict";
        function total() {
            return function n(a, b, c) {
                return a + b + c;
            };
        }""".stripMargin.trim,
      expected = """"use strict";
        function total() {
            return function n(t, r, u) {
                return t + r + u;
            };
        }""".stripMargin.trim,
      options = AllOff.copy(
        keepFargs = true,
        keepFnames = true
      )
    )
  }

}
