/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-1212.
 * Ported from: terser/test/compress/issue-1212.js (2 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue1212Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // issue_1212_debug_false
  // =========================================================================
  test("issue_1212_debug_false".fail) {
    assertCompresses(
      input = """class foo {
            bar() {
                if (DEBUG)
                    console.log("DEV");
                else
                    console.log("PROD");
            }
        }
        new foo().bar()""".stripMargin.trim,
      expected = """class foo{
            bar() { console.log("PROD") }
        }
        (new foo).bar()""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = true,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        properties = true,
        sequencesLimit = 200,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_1212_debug_true
  // =========================================================================
  test("issue_1212_debug_true".fail) {
    assertCompresses(
      input = """class foo {
            bar() {
                if (DEBUG)
                    console.log("DEV");
                else
                    console.log("PROD");
            }
        }
        new foo().bar()""".stripMargin.trim,
      expected = """class foo{
            bar() { console.log("DEV") }
        }
        (new foo).bar()""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = true,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        properties = true,
        sequencesLimit = 200,
        sideEffects = true,
        unused = true
      )
    )
  }

}
