/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-126.
 * Ported from: terser/test/compress/issue-126.js (1 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue126Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // concatenate_rhs_strings
  // =========================================================================
  test("concatenate_rhs_strings".fail) {
    assertCompresses(
      input = """foo(bar() + 123 + "Hello" + "World");
        foo(bar() + (123 + "Hello") + "World");
        foo((bar() + 123) + "Hello" + "World");
        foo(bar() + 123 + "Hello" + "World" + ("Foo" + "Bar"));
        foo("Foo" + "Bar" + bar() + 123 + "Hello" + "World" + ("Foo" + "Bar"));
        foo("Hello" + bar() + 123 + "World");
        foo(bar() + 'Foo' + (10 + parseInt('10')))""".stripMargin.trim,
      expected = """foo(bar() + 123 + "HelloWorld");
        foo(bar() + "123HelloWorld");
        foo((bar() + 123) + "HelloWorld");
        foo(bar() + 123 + "HelloWorldFooBar");
        foo("FooBar" + bar() + "123HelloWorldFooBar");
        foo("Hello" + bar() + "123World");
        foo(bar() + 'Foo' + (10 + parseInt('10')))""".stripMargin.trim,
      options = AllOff.copy(
        evaluate = true,
        unsafe = true
      )
    )
  }

}
