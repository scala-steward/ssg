/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-143.
 * Ported from: terser/test/compress/issue-143.js (4 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue143Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // transformation_sort_order_equal
  // =========================================================================
  test("transformation_sort_order_equal") {
    assertCompresses(
      input = "(a = parseInt('100')) == a",
      expected = "(a = parseInt('100')) == a",
      options = AllOff.copy(
        comparisons = true
      )
    )
  }

  // =========================================================================
  // transformation_sort_order_unequal
  // =========================================================================
  test("transformation_sort_order_unequal") {
    assertCompresses(
      input = "(a = parseInt('100')) != a",
      expected = "(a = parseInt('100')) != a",
      options = AllOff.copy(
        comparisons = true
      )
    )
  }

  // =========================================================================
  // transformation_sort_order_lesser_or_equal
  // =========================================================================
  test("transformation_sort_order_lesser_or_equal") {
    assertCompresses(
      input = "(a = parseInt('100')) <= a",
      expected = "(a = parseInt('100')) <= a",
      options = AllOff.copy(
        comparisons = true
      )
    )
  }

  // =========================================================================
  // transformation_sort_order_greater_or_equal
  // =========================================================================
  test("transformation_sort_order_greater_or_equal") {
    assertCompresses(
      input = "(a = parseInt('100')) >= a",
      expected = "(a = parseInt('100')) >= a",
      options = AllOff.copy(
        comparisons = true
      )
    )
  }

}
