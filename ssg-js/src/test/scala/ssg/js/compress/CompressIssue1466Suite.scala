/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-1466.
 * Ported from: terser/test/compress/issue-1466.js (11 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue1466Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // same_variable_in_multiple_for_loop
  // =========================================================================
  test("same_variable_in_multiple_for_loop".fail) {
    assertCompresses(
      input = """for (let i = 0; i < 3; i++) {
            let a = 100;
            console.log(i, a);
            for (let i = 0; i < 2; i++) {
                console.log(i, a);
                let c = 2;
                console.log(c);
            }
        }""".stripMargin.trim,
      expected = """for (let o = 0; o < 3; o++) {
            let l = 100;
            console.log(o, l);
            for (let o = 0; o < 2; o++) {
                console.log(o, l);
                let e = 2;
                console.log(e);
            }
        }""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = true,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // same_variable_in_multiple_forOf
  // =========================================================================
  test("same_variable_in_multiple_forOf".fail) {
    assertCompresses(
      input = """var test = [ "a", "b", "c" ];
        for (let tmp of test) {
            console.log(tmp);
            let dd;
            dd = [ "e", "f", "g" ];
            for (let tmp of dd) {
                console.log(tmp);
            }
        }""".stripMargin.trim,
      expected = """var test = [ "a", "b", "c" ];
        for (let o of test) {
            console.log(o);
            let e;
            e = [ "e", "f", "g" ];
            for (let o of e)
                console.log(o);
        }""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = true,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // same_variable_in_multiple_forIn
  // =========================================================================
  test("same_variable_in_multiple_forIn".fail) {
    assertCompresses(
      input = """var test = [ "a", "b", "c" ];
        for (let tmp in test) {
            console.log(tmp);
            let dd;
            dd = [ "e", "f", "g" ];
            for (let tmp in test) {
                console.log(tmp);
            }
        }""".stripMargin.trim,
      expected = """var test = [ "a", "b", "c" ];
        for (let e in test) {
            console.log(e);
            let t;
            t = [ "e", "f", "g" ];
            for (let e in test)
                console.log(e);
        }""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = true,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        sideEffects = true,
        unused = false
      )
    )
  }

  // =========================================================================
  // different_variable_in_multiple_for_loop
  // =========================================================================
  test("different_variable_in_multiple_for_loop".fail) {
    assertCompresses(
      input = """for (let i = 0; i < 3; i++) {
            let a = 100;
            console.log(i, a);
            for (let j = 0; j < 2; j++) {
                console.log(j, a);
                let c = 2;
                console.log(c);
            }
        }""".stripMargin.trim,
      expected = """for (let o = 0; o < 3; o++) {
            let l = 100;
            console.log(o, l);
            for (let o = 0; o < 2; o++) {
                console.log(o, l);
                let e = 2;
                console.log(e);
            }
        }""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = true,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // different_variable_in_multiple_forOf
  // =========================================================================
  test("different_variable_in_multiple_forOf".fail) {
    assertCompresses(
      input = """var test = [ "a", "b", "c" ];
        for (let tmp of test) {
            console.log(tmp);
            let dd;
            dd = [ "e", "f", "g" ];
            for (let t of dd) {
                console.log(t);
            }
        }""".stripMargin.trim,
      expected = """var test = [ "a", "b", "c" ];
        for (let o of test) {
            console.log(o);
            let e;
            e = [ "e", "f", "g" ];
            for (let o of e)
                console.log(o);
        }""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = true,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // different_variable_in_multiple_forIn
  // =========================================================================
  test("different_variable_in_multiple_forIn".fail) {
    assertCompresses(
      input = """var test = [ "a", "b", "c" ];
        for (let tmp in test) {
            console.log(tmp);
            let dd;
            dd = [ "e", "f", "g" ];
            for (let t in test) {
                console.log(t);
            }
        }""".stripMargin.trim,
      expected = """var test = [ "a", "b", "c" ];
        for (let e in test) {
            console.log(e);
            let t;
            t = [ "e", "f", "g" ];
            for (let e in test)
                console.log(e);
        }""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = true,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        sideEffects = true,
        unused = false
      )
    )
  }

  // =========================================================================
  // same_variable_in_multiple_forOf_sequences_let
  // =========================================================================
  test("same_variable_in_multiple_forOf_sequences_let".fail) {
    assertCompresses(
      input = """var test = [ "a", "b", "c" ];
        for (let tmp of test) {
            console.log(tmp);
            let dd;
            dd = [ "e", "f", "g" ];
            for (let tmp of dd) {
                console.log(tmp);
            }
        }""".stripMargin.trim,
      expected = """var test = [ "a", "b", "c" ];
        for (let o of test) {
            let e;
            console.log(o), e = [ "e", "f", "g" ];
            for (let o of e)
                console.log(o);
        }""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = true,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        sequencesLimit = 200,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // same_variable_in_multiple_forOf_sequences_const
  // =========================================================================
  test("same_variable_in_multiple_forOf_sequences_const".fail) {
    assertCompresses(
      input = """var test = [ "a", "b", "c" ];
        for (const tmp of test) {
            console.log(tmp);
            let dd;
            dd = [ "e", "f", "g" ];
            for (const tmp of dd) {
                console.log(tmp);
            }
        }""".stripMargin.trim,
      expected = """var test = [ "a", "b", "c" ];
        for (const o of test) {
            let t;
            console.log(o), t = [ "e", "f", "g" ];
            for (const o of t)
                console.log(o);
        }""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = true,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        sequencesLimit = 200,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // same_variable_in_multiple_forIn_sequences_let
  // =========================================================================
  test("same_variable_in_multiple_forIn_sequences_let".fail) {
    assertCompresses(
      input = """var test = [ "a", "b", "c" ];
        for (let tmp in test) {
            console.log(tmp);
            let dd;
            dd = [ "e", "f", "g" ];
            for (let tmp in test) {
                console.log(tmp);
            }
        }""".stripMargin.trim,
      expected = """var test = [ "a", "b", "c" ];
        for (let e in test) {
            let t;
            console.log(e), t = [ "e", "f", "g" ];
            for (let e in test)
                console.log(e);
        }""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = true,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        sequencesLimit = 200,
        sideEffects = true,
        unused = false
      )
    )
  }

  // =========================================================================
  // same_variable_in_multiple_forIn_sequences_const
  // =========================================================================
  test("same_variable_in_multiple_forIn_sequences_const".fail) {
    assertCompresses(
      input = """var test = [ "a", "b", "c" ];
        for (const tmp in test) {
            console.log(tmp);
            let dd;
            dd = [ "e", "f", "g" ];
            for (const tmp in test) {
                console.log(tmp);
            }
        }""".stripMargin.trim,
      expected = """var test = [ "a", "b", "c" ];
        for (const o in test) {
            let t;
            console.log(o), t = [ "e", "f", "g" ];
            for (const o in test)
                console.log(o);
        }""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = true,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        sequencesLimit = 200,
        sideEffects = true,
        unused = false
      )
    )
  }

  // =========================================================================
  // more_variable_in_multiple_for
  // =========================================================================
  test("more_variable_in_multiple_for".fail) {
    assertCompresses(
      input = """for (let a = 9, i = 0; i < 20; i += a) {
            let b = a++ + i;
            console.log(a, b, i);
            for (let k = b, m = b*b, i = 0; i < 10; i++) {
                console.log(a, b, m, k, i);
            }
        }""".stripMargin.trim,
      expected = """for (let o = 9, l = 0; l < 20; l += o) {
            let e = o++ + l;
            console.log(o, e, l);
            for (let l = e, t = e * e, c = 0; c < 10; c++)
                console.log(o, e, t, l, c);
        }""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = true,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        loops = true,
        sideEffects = true,
        unused = false
      )
    )
  }

}
