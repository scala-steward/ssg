/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-1588.
 * Ported from: terser/test/compress/issue-1588.js (3 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue1588Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // safe_undefined
  // =========================================================================
  test("safe_undefined".fail) {
    assertCompresses(
      input = """var a, c;
        console.log(function(undefined) {
            return function() {
                if (a)
                    return b;
                if (c)
                    return d;
            };
        }(1)())""".stripMargin.trim,
      expected = """var a, c;
        console.log(function(n) {
            return function() {
                return a ? b : c ? d : void 0;
            };
        }(1)())""".stripMargin.trim,
      options = AllOff.copy(
        conditionals = true,
        ifReturn = true,
        unsafe = false
      )
    )
  }

  // =========================================================================
  // unsafe_undefined
  // =========================================================================
  test("unsafe_undefined".fail) {
    assertCompresses(
      input = """var a, c;
        console.log(function(undefined) {
            return function() {
                if (a)
                    return b;
                if (c)
                    return d;
            };
        }()())""".stripMargin.trim,
      expected = """var a, c;
        console.log(function(n) {
            return function() {
                return a ? b : c ? d : n;
            };
        }()())""".stripMargin.trim,
      options = AllOff.copy(
        conditionals = true,
        ifReturn = true,
        unsafeUndefined = true
      )
    )
  }

  // =========================================================================
  // runtime_error
  // =========================================================================
  test("runtime_error") {
    assertCompresses(
      input = """const a = 1;
        console.log(a++)""".stripMargin.trim,
      expected = """const a = 1;
        console.log(a++)""".stripMargin.trim,
      options = AllOff
    )
  }

}
