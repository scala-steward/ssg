/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-1750.
 * Ported from: terser/test/compress/issue-1750.js (2 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue1750Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // case_1
  // =========================================================================
  test("case_1") {
    assertCompresses(
      input = """var a = 0, b = 1;
        switch (true) {
          case a || true:
          default:
            b = 2;
          case true:
        }
        console.log(a, b)""".stripMargin.trim,
      expected = """var a = 0, b = 1;
        if (true === (a || true)) b = 2;
        console.log(a, b)""".stripMargin.trim,
      options = AllOff.copy(
        deadCode = true,
        evaluate = true,
        switches = true
      )
    )
  }

  // =========================================================================
  // case_2
  // =========================================================================
  test("case_2") {
    assertCompresses(
      input = """var a = 0, b = 1;
        switch (0) {
          default:
            b = 2;
          case a:
            a = 3;
          case 0:
        }
        console.log(a, b)""".stripMargin.trim,
      expected = """var a = 0, b = 1;
        if (0 === a) a = 3;
        console.log(a, b)""".stripMargin.trim,
      options = AllOff.copy(
        deadCode = true,
        evaluate = true,
        switches = true
      )
    )
  }

}
