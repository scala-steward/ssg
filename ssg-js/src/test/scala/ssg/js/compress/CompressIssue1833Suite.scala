/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-1833.
 * Ported from: terser/test/compress/issue-1833.js (5 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }
import ssg.js.compress.{ ToplevelConfig }

final class CompressIssue1833Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // iife_for
  // =========================================================================
  test("iife_for".fail) {
    assertCompresses(
      input = """function f() {
            function g() {
                L: for (;;) break L;
            }
            g();
        }
        f()""".stripMargin.trim,
      expected = """!function() {
            !function() {
                L: for (;;) break L;
            }();
        }()""".stripMargin.trim,
      options = AllOff.copy(
        negateIife = true,
        reduceFuncs = true,
        reduceVars = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // iife_for_in
  // =========================================================================
  test("iife_for_in".fail) {
    assertCompresses(
      input = """function f() {
            function g() {
                L: for (var a in x) break L;
            }
            g();
        }
        f()""".stripMargin.trim,
      expected = """!function() {
            !function() {
                L: for (var a in x) break L;
            }();
        }()""".stripMargin.trim,
      options = AllOff.copy(
        negateIife = true,
        reduceFuncs = true,
        reduceVars = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // iife_do
  // =========================================================================
  test("iife_do".fail) {
    assertCompresses(
      input = """function f() {
            function g() {
                L: do {
                    break L;
                } while (1);
            }
            g();
        }
        f()""".stripMargin.trim,
      expected = """!function() {
            !function() {
                L: do {
                    break L;
                } while (1);
            }();
        }()""".stripMargin.trim,
      options = AllOff.copy(
        negateIife = true,
        reduceFuncs = true,
        reduceVars = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // iife_while
  // =========================================================================
  test("iife_while".fail) {
    assertCompresses(
      input = """function f() {
            function g() {
                L: while (1) break L;
            }
            g();
        }
        f()""".stripMargin.trim,
      expected = """!function() {
            !function() {
                L: while (1) break L;
            }();
        }()""".stripMargin.trim,
      options = AllOff.copy(
        negateIife = true,
        reduceFuncs = true,
        reduceVars = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // label_do
  // =========================================================================
  test("label_do".fail) {
    assertCompresses(
      input = """L: do {
            continue L;
        } while (0)""".stripMargin.trim,
      expected = """L: do {
            continue L;
        } while (0)""".stripMargin.trim,
      options = AllOff.copy(
        evaluate = true,
        loops = true
      )
    )
  }

}
