/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-203.
 * Ported from: terser/test/compress/issue-203.js (3 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue203Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // compress_new_function
  // =========================================================================
  test("compress_new_function".fail) {
    assertCompresses(
      input = "new Function(\"aa, bb\", 'return aa;')",
      expected = "Function(\"n,r\", \"return n\")",
      options = AllOff.copy(
        unsafe = true,
        unsafeFunction = true
      )
    )
  }

  // =========================================================================
  // compress_new_function_with_destruct
  // =========================================================================
  test("compress_new_function_with_destruct".fail) {
    assertCompresses(
      input = """new Function("aa, [bb]", 'return aa;');
        new Function("aa, {bb}", 'return aa;');
        new Function("[[aa]], [{bb}]", 'return aa;')""".stripMargin.trim,
      expected = """Function("n,[r]", "return n");
        Function("n,{bb:b}", "return n");
        Function("[[n]],[{bb:b}]", "return n")""".stripMargin.trim,
      options = AllOff.copy(
        ecma = 2015,
        unsafe = true,
        unsafeFunction = true
      )
    )
  }

  // =========================================================================
  // compress_new_function_with_destruct_arrows
  // =========================================================================
  test("compress_new_function_with_destruct_arrows".fail) {
    assertCompresses(
      input = """new Function("aa, [bb]", 'return aa;');
        new Function("aa, {bb}", 'return aa;');
        new Function("[[aa]], [{bb}]", 'return aa;')""".stripMargin.trim,
      expected = """Function("n,[a]", "return n");
        Function("b,{bb:n}", "return b");
        Function("[[b]],[{bb:n}]", "return b")""".stripMargin.trim,
      options = AllOff.copy(
        arrows = true,
        ecma = 2015,
        unsafe = true,
        unsafeArrows = true,
        unsafeFunction = true
      )
    )
  }

}
