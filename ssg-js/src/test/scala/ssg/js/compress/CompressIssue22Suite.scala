/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-22.
 * Ported from: terser/test/compress/issue-22.js (1 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue22Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // return_with_no_value_in_if_body
  // =========================================================================
  test("return_with_no_value_in_if_body") {
    assertCompresses(
      input = """function foo(bar) {
            if (bar) {
                return;
            } else {
                return 1;
            }
        }""".stripMargin.trim,
      expected = """function foo (bar) {
            return bar ? void 0 : 1;
        }""".stripMargin.trim,
      options = AllOff.copy(
        conditionals = true
      )
    )
  }

}
