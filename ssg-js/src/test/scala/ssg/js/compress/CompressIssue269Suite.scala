/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-269.
 * Ported from: terser/test/compress/issue-269.js (5 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue269Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // issue_269_1
  // =========================================================================
  test("issue_269_1") {
    assertCompresses(
      input = """f(
            String(x),
            Number(x),
            Boolean(x),

            String(),
            Number(),
            Boolean()
        )""".stripMargin.trim,
      expected = """f(
            x + "", +x, !!x,
            "", 0, false
        )""".stripMargin.trim,
      options = AllOff.copy(
        unsafe = true,
        unsafeMath = true
      )
    )
  }

  // =========================================================================
  // issue_269_dangers
  // =========================================================================
  test("issue_269_dangers") {
    assertCompresses(
      input = """f(
            String(x, x),
            Number(x, x),
            Boolean(x, x)
        )""".stripMargin.trim,
      expected = "f(String(x, x), Number(x, x), Boolean(x, x))",
      options = AllOff.copy(
        unsafe = true
      )
    )
  }

  // =========================================================================
  // issue_269_in_scope
  // =========================================================================
  test("issue_269_in_scope") {
    assertCompresses(
      input = """var String, Number, Boolean;
        f(
            String(x),
            Number(x, x),
            Boolean(x)
        )""".stripMargin.trim,
      expected = """var String, Number, Boolean;
        f(String(x), Number(x, x), Boolean(x))""".stripMargin.trim,
      options = AllOff.copy(
        unsafe = true
      )
    )
  }

  // =========================================================================
  // strings_concat
  // =========================================================================
  test("strings_concat") {
    assertCompresses(
      input = """f(
            String(x + "str"),
            String("str" + x)
        )""".stripMargin.trim,
      expected = """f(
            x + "str",
            "str" + x
        )""".stripMargin.trim,
      options = AllOff.copy(
        unsafe = true
      )
    )
  }

  // =========================================================================
  // regexp
  // =========================================================================
  test("regexp".fail) {
    assertCompresses(
      input = """RegExp("foo");
        RegExp("bar", "ig");
        RegExp(foo);
        RegExp("bar", ig);
        RegExp("should", "fail")""".stripMargin.trim,
      expected = """/foo/;
        /bar/ig;
        RegExp(foo);
        RegExp("bar", ig);
        RegExp("should", "fail")""".stripMargin.trim,
      options = AllOff.copy(
        evaluate = true,
        unsafe = true
      )
    )
  }

}
