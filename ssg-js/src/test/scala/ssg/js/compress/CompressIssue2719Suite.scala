/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-2719.
 * Ported from: terser/test/compress/issue-2719.js (1 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }
import ssg.js.compress.{ InlineLevel, ToplevelConfig }

final class CompressIssue2719Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // warn
  // =========================================================================
  test("warn".fail) {
    assertCompresses(
      input = """function f() {
            return g();
        }
        function g() {
            return g["call" + "er"].arguments;
        }
        // 3
        console.log(f(1, 2, 3).length)""".stripMargin.trim,
      expected = """// TypeError: Cannot read property 'arguments' of null
        console.log(function g() {
            return g.caller.arguments;
        }().length)""".stripMargin.trim,
      options = AllOff.copy(
        evaluate = true,
        inline = InlineLevel.InlineFull,
        passes = 2,
        properties = true,
        reduceFuncs = true,
        reduceVars = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

}
