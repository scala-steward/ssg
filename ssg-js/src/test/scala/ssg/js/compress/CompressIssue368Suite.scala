/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-368.
 * Ported from: terser/test/compress/issue-368.js (1 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue368Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // collapse
  // =========================================================================
  test("collapse".fail) {
    assertCompresses(
      input = """function f1() {
            var a;
            a = typeof b === 'function' ? b() : b;
            return a !== undefined && c();
        }
        function f2(b) {
            var a;
            b = c();
            a = typeof b === 'function' ? b() : b;
            return 'stirng' == typeof a && d();
        }
        function f3(c) {
            var a;
            a = b(a / 2);
            if (a < 0) {
                a++;
                ++c;
                return c / 2;
            }
        }
        function f4(c) {
            var a;
            a = b(a / 2);
            if (a < 0) {
                a++;
                c++;
                return c / 2;
            }
        }""".stripMargin.trim,
      expected = """function f1() {
            return (typeof b === 'function' ? b() : b) !== void 0 && c();
        }
        function f2(b) {
            return 'stirng' == typeof (typeof (b = c()) === 'function' ? b() : b) && d();
        }
        function f3(c) {
            var a;
            if ((a = b(a / 2)) < 0) return a++, ++c / 2;
        }
        function f4(c) {
            var a;
            if ((a = b(a / 2)) < 0) return a++, ++c / 2;
        }""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        sequencesLimit = 200,
        sideEffects = true,
        unused = true
      )
    )
  }

}
