/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-44.
 * Ported from: terser/test/compress/issue-44.js (2 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue44Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // issue_44_valid_ast_1
  // =========================================================================
  test("issue_44_valid_ast_1".fail) {
    assertCompresses(
      input = """function a(b) {
            for (var i = 0, e = b.qoo(); ; i++) {}
        }""".stripMargin.trim,
      expected = """function a(b) {
            var i = 0;
            for (b.qoo(); ; i++);
        }""".stripMargin.trim,
      options = AllOff.copy(
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_44_valid_ast_2
  // =========================================================================
  test("issue_44_valid_ast_2".fail) {
    assertCompresses(
      input = """function a(b) {
            if (foo) for (var i = 0, e = b.qoo(); ; i++) {}
        }""".stripMargin.trim,
      expected = """function a(b) {
            if (foo) {
                var i = 0;
                for (b.qoo(); ; i++);
            }
        }""".stripMargin.trim,
      options = AllOff.copy(
        unused = true
      )
    )
  }

}
