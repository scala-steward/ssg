/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-611.
 * Ported from: terser/test/compress/issue-611.js (1 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue611Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // issue_611
  // =========================================================================
  test("issue_611".fail) {
    assertCompresses(
      input = """define(function() {
      function fn() {}
      if (fn()) {
        fn();
        return void 0;
      }
    })""".stripMargin.trim,
      expected = """define(function() {
      function fn(){}
      if (fn()) return void fn();
    })""".stripMargin.trim,
      options = AllOff.copy(
        sequencesLimit = 200,
        sideEffects = true
      )
    )
  }

}
