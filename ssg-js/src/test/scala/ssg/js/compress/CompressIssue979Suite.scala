/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-979.
 * Ported from: terser/test/compress/issue-979.js (2 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue979Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // issue979_reported
  // =========================================================================
  test("issue979_reported".fail) {
    assertCompresses(
      input = """function f1() {
            if (a == 1 || b == 2) {
                foo();
            }
        }
        function f2() {
            if (!(a == 1 || b == 2)) {
            }
            else {
                foo();
            }
        }""".stripMargin.trim,
      expected = """function f1() {
            1!=a&&2!=b||foo();
        }
        function f2() {
            1!=a&&2!=b||foo();
        }""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        lhsConstants = true,
        loops = true,
        properties = true,
        sequencesLimit = 200,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // issue979_test_negated_is_best
  // =========================================================================
  test("issue979_test_negated_is_best".fail) {
    assertCompresses(
      input = """function f3() {
            if (a == 1 | b == 2) {
                foo();
            }
        }
        function f4() {
            if (!(a == 1 | b == 2)) {
            }
            else {
                foo();
            }
        }
        function f5() {
            if (a == 1 && b == 2) {
                foo();
            }
        }
        function f6() {
            if (!(a == 1 && b == 2)) {
            }
            else {
                foo();
            }
        }
        function f7() {
            if (a == 1 || b == 2) {
                foo();
            }
            else {
                return bar();
            }
        }""".stripMargin.trim,
      expected = """function f3() {
            1==a|2==b&&foo();
        }
        function f4() {
            1==a|2==b&&foo();
        }
        function f5() {
            1==a&&2==b&&foo();
        }
        function f6() {
            1!=a||2!=b||foo();
        }
        function f7() {
            if(1!=a&&2!=b)return bar();foo()
        }""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        comparisons = true,
        conditionals = true,
        deadCode = true,
        evaluate = true,
        hoistFuns = true,
        ifReturn = true,
        joinVars = true,
        keepFargs = true,
        lhsConstants = true,
        loops = true,
        properties = true,
        sequencesLimit = 200,
        sideEffects = true,
        unused = true
      )
    )
  }

}
