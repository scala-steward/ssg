/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for labels.
 * Ported from: terser/test/compress/labels.js (10 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressLabelsSuite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // labels_1
  // =========================================================================
  test("labels_1".fail) {
    assertCompresses(
      input = """out: {
            if (foo) break out;
            console.log("bar");
        }""".stripMargin.trim,
      expected = "foo || console.log(\"bar\")",
      options = AllOff.copy(
        conditionals = true,
        deadCode = true,
        ifReturn = true
      )
    )
  }

  // =========================================================================
  // labels_2
  // =========================================================================
  test("labels_2".fail) {
    assertCompresses(
      input = """out: {
            if (foo) print("stuff");
            else break out;
            console.log("here");
        }""".stripMargin.trim,
      expected = """if (foo) {
            print("stuff");
            console.log("here");
        }""".stripMargin.trim,
      options = AllOff.copy(
        conditionals = true,
        deadCode = true,
        ifReturn = true
      )
    )
  }

  // =========================================================================
  // labels_3
  // =========================================================================
  test("labels_3".fail) {
    assertCompresses(
      input = """for (var i = 0; i < 5; ++i) {
            if (i < 3) continue;
            console.log(i);
        }""".stripMargin.trim,
      expected = """for (var i = 0; i < 5; ++i)
            i < 3 || console.log(i)""".stripMargin.trim,
      options = AllOff.copy(
        conditionals = true,
        deadCode = true,
        ifReturn = true
      )
    )
  }

  // =========================================================================
  // labels_4
  // =========================================================================
  test("labels_4".fail) {
    assertCompresses(
      input = """out: for (var i = 0; i < 5; ++i) {
            if (i < 3) continue out;
            console.log(i);
        }""".stripMargin.trim,
      expected = """for (var i = 0; i < 5; ++i)
            i < 3 || console.log(i)""".stripMargin.trim,
      options = AllOff.copy(
        conditionals = true,
        deadCode = true,
        ifReturn = true
      )
    )
  }

  // =========================================================================
  // labels_5
  // =========================================================================
  test("labels_5") {
    assertCompresses(
      input = """while (foo) {
            if (bar) break;
            console.log("foo");
        }
        out: while (foo) {
            if (bar) break out;
            console.log("foo");
        }""".stripMargin.trim,
      expected = """while (foo) {
            if (bar) break;
            console.log("foo");
        }
        out: while (foo) {
            if (bar) break out;
            console.log("foo");
        }""".stripMargin.trim,
      options = AllOff.copy(
        conditionals = true,
        deadCode = true,
        ifReturn = true
      )
    )
  }

  // =========================================================================
  // labels_6
  // =========================================================================
  test("labels_6".fail) {
    assertCompresses(
      input = "out: break out",
      expected = "",
      options = AllOff
    )
  }

  // =========================================================================
  // labels_7
  // =========================================================================
  test("labels_7".fail) {
    assertCompresses(
      input = """while (foo) {
            x();
            y();
            continue;
        }""".stripMargin.trim,
      expected = """while (foo) {
            x();
            y();
        }""".stripMargin.trim,
      options = AllOff.copy(
        conditionals = true,
        deadCode = true,
        ifReturn = true
      )
    )
  }

  // =========================================================================
  // labels_8
  // =========================================================================
  test("labels_8") {
    assertCompresses(
      input = """while (foo) {
            x();
            y();
            break;
        }""".stripMargin.trim,
      expected = """while (foo) {
            x();
            y();
            break;
        }""".stripMargin.trim,
      options = AllOff.copy(
        conditionals = true,
        deadCode = true,
        ifReturn = true
      )
    )
  }

  // =========================================================================
  // labels_9
  // =========================================================================
  test("labels_9".fail) {
    assertCompresses(
      input = """out: while (foo) {
            x();
            y();
            continue out;
            z();
            k();
        }""".stripMargin.trim,
      expected = """while (foo) {
            x();
            y();
        }""".stripMargin.trim,
      options = AllOff.copy(
        conditionals = true,
        deadCode = true,
        ifReturn = true
      )
    )
  }

  // =========================================================================
  // labels_10
  // =========================================================================
  test("labels_10") {
    assertCompresses(
      input = """out: while (foo) {
            x();
            y();
            break out;
            z();
            k();
        }""".stripMargin.trim,
      expected = """out: while (foo) {
            x();
            y();
            break out;
        }""".stripMargin.trim,
      options = AllOff.copy(
        conditionals = true,
        deadCode = true,
        ifReturn = true
      )
    )
  }

}
