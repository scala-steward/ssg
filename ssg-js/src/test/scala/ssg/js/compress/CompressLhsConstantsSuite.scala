/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for lhs_constants.
 * Ported from: terser/test/compress/lhs_constants.js (1 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressLhsConstantsSuite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // lhs_constants
  // =========================================================================
  test("lhs_constants".fail) {
    assertCompresses(
      input = """function test(a, b, c, d) {
            var x;

            x = a == 42;
            x = a === 42;
            x = a != 42;
            x = a !== 42;
            x = a * 42;
            x = a & 42;
            x = a | 42;
            x = a ^ 42;

            x = a == 42n;
            x = a == "moo";
            x = a == null;

            x = a == ~42;
            x = a == void 0;
            x = a == +42;

            x = a == /moo/;

            a & 42 & 2;
            a == 42 & 2;
        }""".stripMargin.trim,
      expected = """function test(a, b, c, d) {
            var x;

            x = 42 == a;
            x = 42 === a;
            x = 42 != a;
            x = 42 !== a;
            x = 42 * a;
            x = 42 & a;
            x = 42 | a;
            x = 42 ^ a;

            x = 42n == a;
            x = "moo" == a;
            x = null == a;

            x = ~42 == a;
            x = void 0 == a;
            x = +42 == a;

            x = a == /moo/;

            42 & a & 2;
            42 == a & 2;
        }""".stripMargin.trim,
      options = AllOff.copy(
        lhsConstants = true
      )
    )
  }

}
