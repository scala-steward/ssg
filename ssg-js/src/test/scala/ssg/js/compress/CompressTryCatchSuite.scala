/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for try-catch.
 * Ported from: terser/test/compress/try-catch.js (2 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressTryCatchSuite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // broken_safari_catch_scope
  // =========================================================================
  test("broken_safari_catch_scope".fail) {
    assertCompresses(
      input = """"AAAAAAAA";
        "BBBBBBB";
        new class {
            f(x) {
                try {
                    throw {
                        m: "PASS"
                    };
                } catch ({m: s}) {
                    console.log(s);
                }
            }
        }().f()""".stripMargin.trim,
      expected = """"AAAAAAAA";
        "BBBBBBB";
        new class {
            f(A) {
                try {
                    throw {
                        m: "PASS"
                    };
                } catch ({m: B}) {
                    console.log(B);
                }
            }
        }().f()""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // broken_safari_catch_scope_caveat
  // =========================================================================
  test("broken_safari_catch_scope_caveat".fail) {
    assertCompresses(
      input = """"AAAAAAAA";
        "BBBBBBB";
        new class {
            f(x) {
                try {
                    throw {
                        m: "PASS"
                    };
                } catch ({m: x}) {
                    console.log(x);
                }
            }
        }().f()""".stripMargin.trim,
      expected = """"AAAAAAAA";
        "BBBBBBB";
        new class {
            f(A) {
                try {
                    throw {
                        m: "PASS"
                    };
                } catch ({m: A}) {
                    console.log(A);
                }
            }
        }().f()""".stripMargin.trim,
      options = AllOff
    )
  }

}
