/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for export.
 * Ported from: terser/test/compress/export.js (33 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }
import ssg.js.compress.ToplevelConfig

final class CompressExportSuite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // issue_2038_1
  // =========================================================================
  test("issue_2038_1") {
    assertCompresses(
      input = """export var V = 1;
        export let L = 2;
        export const C = 3""".stripMargin.trim,
      expected = """export var V = 1;
        export let L = 2;
        export const C = 3""".stripMargin.trim,
      options = AllOff.copy(
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2038_2
  // =========================================================================
  test("issue_2038_2".fail) {
    assertCompresses(
      input = """let LET = 1;
        const CONST = 2;
        var VAR = 3;
        export { LET, CONST, VAR }""".stripMargin.trim,
      expected = """let t = 1;
        const e = 2;
        var o = 3;
        export { t as LET, e as CONST, o as VAR }""".stripMargin.trim,
      options = AllOff.copy(
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2126
  // =========================================================================
  test("issue_2126".fail) {
    assertCompresses(
      input = """import { foo as bar, cat as dog } from "stuff";
        console.log(bar, dog);
        export { bar as qux };
        export { dog }""".stripMargin.trim,
      expected = """import { foo as o, cat as s } from "stuff";
        console.log(o, s);
        export { o as qux };
        export { s as dog }""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // issue_2131
  // =========================================================================
  test("issue_2131".fail) {
    assertCompresses(
      input = """function no() {
            console.log(42);
        }
        function go() {
            console.log(42);
        }
        var X = 1, Y = 2;
        export function main() {
            go(X);
        }""".stripMargin.trim,
      expected = """function go() {
            console.log(42);
        }
        var X = 1;
        export function main() {
            go(X);
        }""".stripMargin.trim,
      options = AllOff.copy(
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2129
  // =========================================================================
  test("issue_2129") {
    assertCompresses(
      input = "export const { keys } = Object",
      expected = "export const { keys } = Object",
      options = AllOff
    )
  }

  // =========================================================================
  // async_func
  // =========================================================================
  test("async_func") {
    assertCompresses(
      input = "export async function Foo(x){}",
      expected = "export async function Foo(){}",
      options = AllOff.copy(
        keepFargs = false,
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2134_1
  // =========================================================================
  test("issue_2134_1") {
    assertCompresses(
      input = """export function Foo(x){};
        Foo.prototype = {}""".stripMargin.trim,
      expected = """export function Foo(){};
        Foo.prototype = {}""".stripMargin.trim,
      options = AllOff.copy(
        keepFargs = false,
        reduceFuncs = true,
        reduceVars = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2134_2
  // =========================================================================
  test("issue_2134_2") {
    assertCompresses(
      input = """export async function Foo(x){};
        Foo.prototype = {}""".stripMargin.trim,
      expected = """export async function Foo(){};
        Foo.prototype = {}""".stripMargin.trim,
      options = AllOff.copy(
        keepFargs = false,
        reduceFuncs = true,
        reduceVars = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // redirection
  // =========================================================================
  test("redirection".fail) {
    assertCompresses(
      input = """let foo = 1, bar = 2;
        export { foo as delete };
        export { bar as default };
        export { foo as var } from "module.js"""".stripMargin.trim,
      expected = """let e = 1, o = 2;
        export { e as delete };
        export { o as default };
        export { foo as var } from "module.js"""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // keyword_invalid_1
  // =========================================================================
  test("keyword_invalid_1") {
    assertCompresses(
      input = "export { default }",
      expected = "export { default }",
      options = AllOff
    )
  }

  // =========================================================================
  // keyword_invalid_2
  // =========================================================================
  test("keyword_invalid_2") {
    assertCompresses(
      input = "export { default as Alias }",
      expected = "export { default as Alias }",
      options = AllOff
    )
  }

  // =========================================================================
  // keyword_invalid_3
  // =========================================================================
  test("keyword_invalid_3") {
    assertCompresses(
      input = "export { default as default }",
      expected = "export { default as default }",
      options = AllOff
    )
  }

  // =========================================================================
  // keyword_valid_1
  // =========================================================================
  test("keyword_valid_1") {
    assertCompresses(
      input = "export { default } from \"module.js\"",
      expected = "export { default } from \"module.js\"",
      options = AllOff
    )
  }

  // =========================================================================
  // keyword_valid_2
  // =========================================================================
  test("keyword_valid_2") {
    assertCompresses(
      input = "export { default as Alias } from \"module.js\"",
      expected = "export { default as Alias } from \"module.js\"",
      options = AllOff
    )
  }

  // =========================================================================
  // keyword_valid_3
  // =========================================================================
  test("keyword_valid_3") {
    assertCompresses(
      input = "export { default as default } from \"module.js\"",
      expected = "export { default as default } from \"module.js\"",
      options = AllOff
    )
  }

  // =========================================================================
  // dynamic_import
  // =========================================================================
  test("dynamic_import".fail) {
    assertCompresses(
      input = """import traditional from './traditional.js';
        function imp(x) {
            return import(x);
        }
        import("module_for_side_effects.js");
        let dynamic = import("some/module.js");
        dynamic.foo()""".stripMargin.trim,
      expected = """import o from "./traditional.js";
        function t(o) {
            return import(o);
        }
        import("module_for_side_effects.js");
        let r = import("some/module.js");
        r.foo()""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // name_cache_do_not_mangle_export_function_name
  // =========================================================================
  test("name_cache_do_not_mangle_export_function_name".fail) {
    assertCompresses(
      input = """export function add(x, y) { return x + y; }
        function sub(x, y) { return x - y; }
        console.log(add(1, 2), add(3, 4), sub(5, 6), sub(7, 8))""".stripMargin.trim,
      expected = """export function add(n, d) {
            return n + d;
        }
        function _$$SUB$$_(n, d) {
            return n - d;
        }
        console.log(add(1, 2), add(3, 4), _$$SUB$$_(5, 6), _$$SUB$$_(7, 8))""".stripMargin.trim,
      options = AllOff.copy(
        module = true
      )
    )
  }

  // =========================================================================
  // name_cache_do_not_mangle_export_class_name
  // =========================================================================
  test("name_cache_do_not_mangle_export_class_name".fail) {
    assertCompresses(
      input = """export class add {}
        class sub {}
        console.log(add, add, sub, sub)""".stripMargin.trim,
      expected = """export class add {}
        class _$$SUB$$_ {}
        console.log(add, add, _$$SUB$$_, _$$SUB$$_)""".stripMargin.trim,
      options = AllOff.copy(
        module = true
      )
    )
  }

  // =========================================================================
  // name_cache_do_not_mangle_export_var_name
  // =========================================================================
  test("name_cache_do_not_mangle_export_var_name".fail) {
    assertCompresses(
      input = """export var add = 1;
        var sub = 2, mul = 3;
        console.log(add, add, sub, sub, mul, mul)""".stripMargin.trim,
      expected = """export var add = 1;
        var _$$SUB$$_ = 2, d = 3;
        console.log(add, add, _$$SUB$$_, _$$SUB$$_, d, d)""".stripMargin.trim,
      options = AllOff.copy(
        reduceVars = false,
        toplevel = ToplevelConfig(funcs = true, vars = true)
      )
    )
  }

  // =========================================================================
  // name_cache_do_not_mangle_export_let_name
  // =========================================================================
  test("name_cache_do_not_mangle_export_let_name".fail) {
    assertCompresses(
      input = """export let add = 1;
        let sub = 2, mul = 3;
        console.log(add, add, sub, sub, mul, mul)""".stripMargin.trim,
      expected = """export let add = 1;
        let _$$SUB$$_ = 2, d = 3;
        console.log(add, add, _$$SUB$$_, _$$SUB$$_, d, d)""".stripMargin.trim,
      options = AllOff.copy(
        reduceVars = false,
        toplevel = ToplevelConfig(funcs = true, vars = true)
      )
    )
  }

  // =========================================================================
  // name_cache_do_not_mangle_export_const_name
  // =========================================================================
  test("name_cache_do_not_mangle_export_const_name".fail) {
    assertCompresses(
      input = """export const add = 1;
        const sub = 2, mul = 3;
        console.log(add, add, sub, sub, mul, mul)""".stripMargin.trim,
      expected = """export const add = 1;
        const _$$SUB$$_ = 2, d = 3;
        console.log(add, add, _$$SUB$$_, _$$SUB$$_, d, d)""".stripMargin.trim,
      options = AllOff.copy(
        reduceVars = false,
        toplevel = ToplevelConfig(funcs = true, vars = true)
      )
    )
  }

  // =========================================================================
  // name_cache_do_not_mangle_export_destructuring_name
  // =========================================================================
  test("name_cache_do_not_mangle_export_destructuring_name".fail) {
    assertCompresses(
      input = """export const [add] = [1, 2, 3];
        const [mul, sub] = [1, 2, 3];
        console.log(add, add, sub, sub, mul, mul)""".stripMargin.trim,
      expected = """export const [add] = [ 1, 2, 3 ];
        const [d, _$$SUB$$_] = [ 1, 2, 3 ];
        console.log(add, add, _$$SUB$$_, _$$SUB$$_, d, d)""".stripMargin.trim,
      options = AllOff.copy(
        module = true
      )
    )
  }

  // =========================================================================
  // name_cache_do_not_mangle_export_from_names
  // =========================================================================
  test("name_cache_do_not_mangle_export_from_names".fail) {
    assertCompresses(
      input = """// these symbols are unrelated to the `export {} from` statement
        function add() { console.log("should be dropped"); }
        function div() { console.log("should be dropped"); }
        function mul() { console.log("should be dropped"); }
        function divide() { console.log("should be dropped"); }
        function minus() { console.log("should be dropped"); }
        function keep() { console.log("should be kept"); }

        export { add, div as divide, sub as minus, mul } from "path";
        export { keep }""".stripMargin.trim,
      expected = """function _$$KEEP$$_() { console.log("should be kept"); }
        export { add, div as divide, sub as minus, mul } from "path";
        export { _$$KEEP$$_ as keep }""".stripMargin.trim,
      options = AllOff.copy(
        module = true
      )
    )
  }

  // =========================================================================
  // name_cache_mangle_export_default_class
  // =========================================================================
  test("name_cache_mangle_export_default_class".fail) {
    assertCompresses(
      input = """export default class foo {}
        export class bar {}
        class baz {
            meth() {}
        }
        class qux {}
        console.log(foo, bar, baz, qux, qux)""".stripMargin.trim,
      expected = """export default class _$$FOO$$_ {}
        export class bar {}
        class _$$QUX$$_ {}
        console.log(_$$FOO$$_, bar, class {
            meth() {}
        }, _$$QUX$$_, _$$QUX$$_)""".stripMargin.trim,
      options = AllOff.copy(
        module = true
      )
    )
  }

  // =========================================================================
  // module_mangle_export_default_class
  // =========================================================================
  test("module_mangle_export_default_class".fail) {
    assertCompresses(
      input = """export default class foo {}
        export class bar {}
        class baz { meth(){} }
        class qux {}
        console.log(foo, bar, baz, qux)""".stripMargin.trim,
      expected = """export default class s {}
        export class bar {}
        console.log(s, bar, class { meth(){} }, class {})""".stripMargin.trim,
      options = AllOff.copy(
        module = true,
        passes = 3
      )
    )
  }

  // =========================================================================
  // name_cache_mangle_export_default_function
  // =========================================================================
  test("name_cache_mangle_export_default_function".fail) {
    assertCompresses(
      input = """export default function foo() {
            return 1;
        }
        export function bar() {
            return 2;
        }
        function qux() {
            return 3;
        }
        console.log(foo(), bar(), qux())""".stripMargin.trim,
      expected = """export default function _$$FOO$$_() {
            return 1;
        }
        export function bar() {
            return 2;
        }
        console.log(_$$FOO$$_(), bar(), 3)""".stripMargin.trim,
      options = AllOff.copy(
        module = true,
        passes = 3
      )
    )
  }

  // =========================================================================
  // module_mangle_export_default_function
  // =========================================================================
  test("module_mangle_export_default_function".fail) {
    assertCompresses(
      input = """export default function foo() {
            return 1;
        }
        export function bar() {
            return 2;
        }
        function qux() {
            return 3;
        }
        console.log(foo(), bar(), qux())""".stripMargin.trim,
      expected = """export default function r() {
            return 1;
        }
        export function bar() {
            return 2;
        }
        console.log(r(), bar(), 3)""".stripMargin.trim,
      options = AllOff.copy(
        module = true,
        passes = 3
      )
    )
  }

  // =========================================================================
  // name_cache_mangle_local_import_and_export_aliases
  // =========================================================================
  test("name_cache_mangle_local_import_and_export_aliases".fail) {
    assertCompresses(
      input = """import { foo as bar, cat as dog, bird } from "stuff";
        console.log(bar, dog, bird);
        export { bar as qux, dog, bird }""".stripMargin.trim,
      expected = """import { foo as _$$BAR$$_, cat as _$$DOG$$_, bird as _$$BIRD$$_ } from "stuff";
        console.log(_$$BAR$$_, _$$DOG$$_, _$$BIRD$$_);
        export { _$$BAR$$_ as qux, _$$DOG$$_ as dog, _$$BIRD$$_ as bird }""".stripMargin.trim,
      options = AllOff.copy(
        module = true
      )
    )
  }

  // =========================================================================
  // name_cache_import_star_as_name_from_module
  // =========================================================================
  test("name_cache_import_star_as_name_from_module".fail) {
    assertCompresses(
      input = """import * as fs from "filesystem";
        import * as stuff from "whatever";
        fs.resolve();
        stuff.search();
        export { fs, stuff }""".stripMargin.trim,
      expected = """import * as _$$FS$$_ from "filesystem";
        import * as e from "whatever";
        _$$FS$$_.resolve(), e.search();
        export { _$$FS$$_ as fs, e as stuff }""".stripMargin.trim,
      options = AllOff.copy(
        module = true
      )
    )
  }

  // =========================================================================
  // issue_333
  // =========================================================================
  test("issue_333") {
    assertCompresses(
      input = """function shortOut() {
            return function() {};
        }

        var setToString = shortOut();

        var _setToString = setToString;

        export function baseRest() {
            return _setToString();
        }

        export { _setToString }""".stripMargin.trim,
      expected = """function shortOut() {
            return function () {};
        }

        var setToString = shortOut();
        var _setToString = setToString;

        export function baseRest() {
            return _setToString();
        }

        export { _setToString }""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true
      )
    )
  }

  // =========================================================================
  // issue_333_toplevel
  // =========================================================================
  test("issue_333_toplevel".fail) {
    assertCompresses(
      input = """function shortOut() {
            return function() {};
        }

        var setToString = shortOut();

        var _setToString = setToString;

        export function baseRest() {
            return _setToString();
        }

        export { _setToString }""".stripMargin.trim,
      expected = """var _setToString = function () {};

        export function baseRest() {
            return _setToString();
        }

        export { _setToString }""".stripMargin.trim,
      options = AllOff.copy(
        toplevel = ToplevelConfig(funcs = true, vars = true)
      )
    )
  }

  // =========================================================================
  // export_object_property_mangle
  // =========================================================================
  test("export_object_property_mangle") {
    assertCompresses(
      input = """const n = null;
        export const o = { o: n }""".stripMargin.trim,
      expected = """const n = null;
        export const o = {o: n}""".stripMargin.trim,
      options = AllOff.copy(
        reduceVars = false
      )
    )
  }

  // =========================================================================
  // export_object_property_mangle_2
  // =========================================================================
  test("export_object_property_mangle_2") {
    assertCompresses(
      input = """const n = null;
        const o = { o: n };
        export { o }""".stripMargin.trim,
      expected = """const n = null;
        const o = {o: n};
        export {o}""".stripMargin.trim,
      options = AllOff
    )
  }

}
