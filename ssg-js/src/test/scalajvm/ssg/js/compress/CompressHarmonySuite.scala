/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for harmony.
 * Ported from: terser/test/compress/harmony.js (49 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }
import ssg.js.compress.{ InlineLevel, ToplevelConfig }

final class CompressHarmonySuite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // class_name_can_be_mangled
  // =========================================================================
  test("class_name_can_be_mangled".fail) {
    assertCompresses(
      input = """function x() {
            class Foo {
            }
            var class1 = Foo;
            var class2 = class Bar {};
        }""".stripMargin.trim,
      expected = """function x() {
            class a { }
            var s = a;
            var c = class a {};
        }""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // class_name_can_be_preserved
  // =========================================================================
  test("class_name_can_be_preserved") {
    assertCompresses(
      input = """function x() {
            (class Baz { });
            class Foo {};
        }""".stripMargin.trim,
      expected = """function x() {
            (class Baz { });
            class Foo {};
        }""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // classes_can_have_generators
  // =========================================================================
  test("classes_can_have_generators".fail) {
    assertCompresses(
      input = """class Foo {
            *bar() {}
            static *baz() {}
        }""".stripMargin.trim,
      expected = """class Foo {
            *bar() {}
            static *baz() {}
        }""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // classes_can_have_computed_generators
  // =========================================================================
  test("classes_can_have_computed_generators".fail) {
    assertCompresses(
      input = """class C4 {
            *['constructor']() {}
        }""".stripMargin.trim,
      expected = """class C4 {
            *['constructor']() {}
        }""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // classes_can_have_computed_static
  // =========================================================================
  test("classes_can_have_computed_static".fail) {
    assertCompresses(
      input = """class C4 {
            static ['constructor']() {}
        }""".stripMargin.trim,
      expected = """class C4 {
            static ['constructor']() {}
        }""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // classes_extending_classes_out_of_pure_iifes
  // =========================================================================
  test("classes_extending_classes_out_of_pure_iifes".fail) {
    assertCompresses(
      input = """let Base = /*@__PURE__*/ (() => {
            class A {}
            A.sub = Sub
            return A;
        })();

        class Sub extends Base { }""".stripMargin.trim,
      expected = "",
      options = AllOff.copy(
        sideEffects = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // number_literals
  // =========================================================================
  test("number_literals".fail) {
    assertCompresses(
      input = """0b1001;
        0B1001;
        0o11;
        0O11""".stripMargin.trim,
      expected = """9;
        9;
        9;
        9""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // import_statement_mangling
  // =========================================================================
  test("import_statement_mangling".fail) {
    assertCompresses(
      input = """import Foo from "foo";
        import Bar, {Food} from "lel";
        import {What as Whatever} from "lel";
        Foo();
        Bar();
        Food();
        Whatever()""".stripMargin.trim,
      expected = """import o from "foo";
        import m, {Food as r} from "lel";
        import {What as f} from "lel";
        o();
        m();
        r();
        f()""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // export_statement_mangling
  // =========================================================================
  test("export_statement_mangling") {
    assertCompresses(
      input = """export var foo = 6;
        export function bar() { }
        export class Baz { }
        bar(foo, Baz)""".stripMargin.trim,
      expected = """export var foo = 6;
        export function bar() { }
        export class Baz { }
        bar(foo, Baz)""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // regression_for_of_const
  // =========================================================================
  test("regression_for_of_const".fail) {
    assertCompresses(
      input = """for (const x of y) {}
        for (const x in y) {}""".stripMargin.trim,
      expected = "for (const x of y);for (const x in y)",
      options = AllOff
    )
  }

  // =========================================================================
  // regression_cannot_use_of
  // =========================================================================
  test("regression_cannot_use_of") {
    assertCompresses(
      input = """function of() {
        }
        var of = "is a valid variable name";
        of = { of: "is ok" };
        x.of;
        of: foo()""".stripMargin.trim,
      expected = """function of(){}
        var of="is a valid variable name";
        of={of:"is ok"};
        x.of;
        foo();  /* Label statement missing? No prob. */""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // default_assign
  // =========================================================================
  test("default_assign".fail) {
    assertCompresses(
      input = """function f(a, b = 3) {
            console.log(a);
        }

        g = ([[] = 123]) => {};
        h = ([[x, y, z] = [4, 5, 6]] = []) => {};

        function i([[x, y, z] = [4, 5, 6]] = []) {
            console.log(b);
        }""".stripMargin.trim,
      expected = """function f(a) {
            console.log(a);
        }

        g = ([[] = 123]) => {};
        h = ([[x, y, z] = [4, 5, 6]] = []) => {};

        function i([[x, y, z] = [4, 5, 6]] = []) {
            console.log(b);
        }""".stripMargin.trim,
      options = AllOff.copy(
        keepFargs = false,
        unused = true
      )
    )
  }

  // =========================================================================
  // expansion
  // =========================================================================
  test("expansion") {
    assertCompresses(
      input = """function f(a, ...b) {
            console.log(a);
        }""".stripMargin.trim,
      expected = """function f(a) {
            console.log(a);
        }""".stripMargin.trim,
      options = AllOff.copy(
        keepFargs = false,
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_1898
  // =========================================================================
  test("issue_1898".fail) {
    assertCompresses(
      input = """class Foo {
            bar() {
                for (const x of [ 6, 5 ]) {
                    for (let y of [ 4, 3 ]) {
                        for (var z of [ 2, 1 ]) {
                            console.log(x, y, z);
                        }
                    }
                }
            }
        }
        new Foo().bar()""".stripMargin.trim,
      expected = """class Foo {
            bar() {
                for (const f of [ 6, 5 ])
                    for (let r of [ 4, 3 ])
                        for (var o of [ 2, 1 ])
                            console.log(f, r, o);
            }
        }
        new Foo().bar()""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // issue_1753
  // =========================================================================
  test("issue_1753".fail) {
    assertCompresses(
      input = """class SomeClass {
            constructor(props) {
                let pickedSets = [];
                for (let i = 0; i < 6; i++) {
                    pickedSets.push({
                        mainDrawNumbers: [],
                        extraDrawNumbers: []
                    });
                }
            }
        }""".stripMargin.trim,
      expected = """class SomeClass {
            constructor(r) {
                let s = [];
                for (let a = 0; a < 6; a++)
                    s.push({
                        mainDrawNumbers: [],
                        extraDrawNumbers: []
                    });
            }
        }""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // issue_1753_disable
  // =========================================================================
  test("issue_1753_disable".fail) {
    assertCompresses(
      input = """class SomeClass {
            constructor(props) {
                let pickedSets = [];
                for (let i = 0; i < 6; i++) {
                    pickedSets.push({
                        mainDrawNumbers: [],
                        extraDrawNumbers: []
                    });
                }
            }
        }""".stripMargin.trim,
      expected = """class SomeClass {
            constructor(r) {
                let s = [];
                for (let r = 0; r < 6; r++)
                    s.push({
                        mainDrawNumbers: [],
                        extraDrawNumbers: []
                    });
            }
        }""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // issue_2028
  // =========================================================================
  test("issue_2028".fail) {
    assertCompresses(
      input = """var a = {};
        (function(x) {
            x.X = function() {
                return X;
            };
            class X {
                static hello() {
                    console.log("hello");
                }
            }
        }(a));
        a.X().hello()""".stripMargin.trim,
      expected = """var a = {};
        (function(x) {
            x.X = function() {
                return X;
            };
            class X {
                static hello() {
                    console.log("hello");
                }
            }
        }(a));
        a.X().hello()""".stripMargin.trim,
      options = AllOff.copy(
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // object_rest_spread
  // =========================================================================
  test("object_rest_spread".fail) {
    assertCompresses(
      input = """var   { w: w1, ...V } = { w: 7, x: 1, y: 2 }; console.log(w1, V);
        let   { w: w2, ...L } = { w: 8, x: 3, y: 4 }; console.log(w2, L);
        const { w: w3, ...C } = { w: 9, x: 5, y: 6 }; console.log(w3, C);

        let b;
        ({ b, ...V } = { a: 1, b: 2, c: 3 }); console.log(V);
        ({ b, ...L } = { a: 4, b: 5, c: 6 }); console.log(L);

        (function({ y, ...p }){ console.log(p); })({ x: 1, y: 2, z: 3 });
        (({ y, ...p }) =>     { console.log(p); })({ x: 4, y: 5, z: 6 });

        const T = { a: 1, b: 2 }; console.log({ ...T, w: 0, ...{}, ...L, ...{K: 9} })""".stripMargin.trim,
      expected = """var   { w: o, ...l } = { w: 7, x: 1, y: 2 }; console.log(o, l);
        let   { w: c, ...n } = { w: 8, x: 3, y: 4 }; console.log(c, n);
        const { w: e, ...s } = { w: 9, x: 5, y: 6 }; console.log(e, s);

        let g;
        ({ b: g, ...l } = { a: 1, b: 2, c: 3 }); console.log(l);
        ({ b: g, ...n } = { a: 4, b: 5, c: 6 }); console.log(n);

        (function({ y: o, ...l }) { console.log(l); })({ x: 1, y: 2, z: 3 });
        (({ y: o, ...l }) =>      { console.log(l); })({ x: 4, y: 5, z: 6 });

        const w = { a: 1, b: 2 }; console.log({ ...w, w: 0, ...n, K: 9 })""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // object_spread_unsafe
  // =========================================================================
  test("object_spread_unsafe".fail) {
    assertCompresses(
      input = """var o1 = { x: 1, y: 2 };
        var o2 = { x: 3, z: 4 };
        var cloned = { ...o1 };
        var merged = { ...o1, ...o2 };
        console.log(cloned, merged)""".stripMargin.trim,
      expected = """var o = { x: 1, y: 2 }, l = { ...o }, x = { ...o, x: 3, z: 4 };
        console.log(l, x)""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        evaluate = true,
        joinVars = true,
        passes = 3,
        reduceFuncs = true,
        reduceVars = true,
        sideEffects = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unsafe = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // array_spread_of_sequence
  // =========================================================================
  test("array_spread_of_sequence".fail) {
    assertCompresses(
      input = """var a = [1];
        console.log([...(a, a)]);
        console.log([...a, a]);
        console.log([...(a || a)]);
        console.log([...a || a])""".stripMargin.trim,
      expected = """var o = [1];
        console.log([...(o, o)]);
        console.log([...o, o]);
        console.log([...o || o]);
        console.log([...o || o])""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // object_spread_of_sequence
  // =========================================================================
  test("object_spread_of_sequence".fail) {
    assertCompresses(
      input = """var a = {x: 1};
        console.log({ ...(a, a) });
        console.log({ ...a, a });
        console.log({ ...(a || a) });
        console.log({ ...a || a })""".stripMargin.trim,
      expected = """var o = { x: 1 };
        console.log({ ...(o, o) });
        console.log({ ...o, a: o });
        console.log({ ...o || o });
        console.log({ ...o || o })""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // class_name_can_be_preserved_with_reserved
  // =========================================================================
  test("class_name_can_be_preserved_with_reserved".fail) {
    assertCompresses(
      input = """function x() {
            class Foo {}
            Foo.bar;
            class Bar {}
            Bar.foo;
        }
        function y() {
            var Foo = class Foo {};
            Foo.bar;
            var Bar = class Bar {};
            Bar.bar;
        }""".stripMargin.trim,
      expected = """function x() {
            class Foo {}
            Foo.bar;
            class a {}
            a.foo;
        }
        function y() {
            var Foo = class Foo {};
            Foo.bar;
            var a = class a {};
            a.bar;
        }""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // issue_2345
  // =========================================================================
  test("issue_2345".fail) {
    assertCompresses(
      input = """console.log([...[3, 2, 1]].join("-"));
        var a = [3, 2, 1];
        console.log([...a].join("-"))""".stripMargin.trim,
      expected = """console.log("3-2-1");
        var a = [3, 2, 1];
        console.log([...a].join("-"))""".stripMargin.trim,
      options = AllOff.copy(
        evaluate = true,
        sideEffects = true,
        unsafe = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2349
  // =========================================================================
  test("issue_2349".fail) {
    assertCompresses(
      input = """function foo(boo, key) {
            const value = boo.get();
            return value.map(({[key]: bar}) => bar);
        }
        console.log(foo({
            get: () => [ {
                blah: 42
            } ]
        }, "blah"))""".stripMargin.trim,
      expected = """function foo(o, n) {
            const t = o.get();
            return t.map(({[n]: o}) => o);
        }
        console.log(foo({
            get: () => [ {
                blah: 42
            } ]
        }, "blah"))""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // issue_2349b
  // =========================================================================
  test("issue_2349b".fail) {
    assertCompresses(
      input = """function foo(boo, key) {
            const value = boo.get();
            return value.map(function({[key]: bar}){ return bar; });
        }
        console.log(foo({
            get: function() {
                return  [ {
                    blah: 42
                } ];
            }
        }, "blah"))""".stripMargin.trim,
      expected = """console.log((
            a = "blah",
            [{ blah: 42 }].map(({[a]: l}) => l)
        ));
        var a""".stripMargin.trim,
      options = AllOff.copy(
        arrows = true,
        collapseVars = true,
        ecma = 2015,
        evaluate = true,
        inline = InlineLevel.InlineFull,
        passes = 3,
        properties = true,
        reduceFuncs = true,
        reduceVars = true,
        sideEffects = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unsafeArrows = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // array_literal_with_spread_1
  // =========================================================================
  test("array_literal_with_spread_1".fail) {
    assertCompresses(
      input = """var f = (x) => [...x][0];
        console.log(f(["PASS"]))""".stripMargin.trim,
      expected = """var f = x => [ ...x ][0];
        console.log(f([ "PASS" ]))""".stripMargin.trim,
      options = AllOff.copy(
        properties = true,
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // array_literal_with_spread_2a
  // =========================================================================
  test("array_literal_with_spread_2a".fail) {
    assertCompresses(
      input = """console.log([10, ...[], 20, ...[30, 40], 50]["length"]);
        console.log([10, ...[], 20, ...[30, 40], 50][0]);
        console.log([10, ...[], 20, ...[30, 40], 50][1]);
        console.log([10, ...[], 20, ...[30, 40], 50][2]);
        console.log([10, ...[], 20, ...[30, 40], 50][3]);
        console.log([10, ...[], 20, ...[30, 40], 50][4]);
        console.log([10, ...[], 20, ...[30, 40], 50][5])""".stripMargin.trim,
      expected = """console.log([ 10, 20, 30, 40, 50 ]["length"]);
        console.log(10);
        console.log(20);
        console.log(30);
        console.log(40);
        console.log(50);
        console.log([ 10, 20, 30, 40, 50 ][5])""".stripMargin.trim,
      options = AllOff.copy(
        properties = true,
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // array_literal_with_spread_2b
  // =========================================================================
  test("array_literal_with_spread_2b".fail) {
    assertCompresses(
      input = """var x = [30, 40];
        console.log([10, ...[], 20, ...x, 50]["length"]);
        console.log([10, ...[], 20, ...x, 50][0]);
        console.log([10, ...[], 20, ...x, 50][1]);
        console.log([10, ...[], 20, ...x, 50][2]);
        console.log([10, ...[], 20, ...x, 50][3]);
        console.log([10, ...[], 20, ...x, 50][4]);
        console.log([10, ...[], 20, ...x, 50][5])""".stripMargin.trim,
      expected = """var x = [30, 40];
        console.log([ 10, 20, ...x, 50 ]["length"]);
        console.log(10);
        console.log(20);
        console.log([ 10, 20, ...x, 50 ][2]);
        console.log([ 10, 20, ...x, 50 ][3]);
        console.log([ 10, 20, ...x, 50 ][4]);
        console.log([ 10, 20, ...x, 50 ][5])""".stripMargin.trim,
      options = AllOff.copy(
        properties = true,
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // array_literal_with_spread_3a
  // =========================================================================
  test("array_literal_with_spread_3a".fail) {
    assertCompresses(
      input = """console.log([10, 20][0]);
        console.log([10, 20][1]);
        console.log([10, 20][2]);

        console.log([...[], 10, 20][0]);
        console.log([...[], 10, 20][1]);
        console.log([...[], 10, 20][2]);

        console.log([10, ...[], 20][0]);
        console.log([10, ...[], 20][1]);
        console.log([10, ...[], 20][2]);

        console.log([10, 20, ...[]][0]);
        console.log([10, 20, ...[]][1]);
        console.log([10, 20, ...[]][2])""".stripMargin.trim,
      expected = """console.log(10);
        console.log(20);
        console.log([ 10, 20 ][2]);

        console.log(10);
        console.log(20);
        console.log([10, 20][2]);

        console.log(10);
        console.log(20);
        console.log([10, 20][2]);

        console.log(10);
        console.log(20);
        console.log([10, 20][2])""".stripMargin.trim,
      options = AllOff.copy(
        properties = true,
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // array_literal_with_spread_3b
  // =========================================================================
  test("array_literal_with_spread_3b".fail) {
    assertCompresses(
      input = """var nothing = [];
        console.log([10, 20][0]);
        console.log([10, 20][1]);
        console.log([10, 20][2]);

        console.log([...nothing, 10, 20][0]);
        console.log([...nothing, 10, 20][1]);
        console.log([...nothing, 10, 20][2]);

        console.log([10, ...nothing, 20][0]);
        console.log([10, ...nothing, 20][1]);
        console.log([10, ...nothing, 20][2]);

        console.log([10, 20, ...nothing][0]);
        console.log([10, 20, ...nothing][1]);
        console.log([10, 20, ...nothing][2])""".stripMargin.trim,
      expected = """var nothing = [];
        console.log(10);
        console.log(20);
        console.log([ 10, 20 ][2]);

        console.log([...nothing, 10, 20][0]);
        console.log([...nothing, 10, 20][1]);
        console.log([...nothing, 10, 20][2]);

        console.log(10);
        console.log([10, ...nothing, 20][1]);
        console.log([10, ...nothing, 20][2]);

        console.log(10);
        console.log(20);
        console.log([10, 20, ...nothing][2])""".stripMargin.trim,
      options = AllOff.copy(
        properties = true,
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // array_literal_with_spread_4a
  // =========================================================================
  test("array_literal_with_spread_4a".fail) {
    assertCompresses(
      input = """function t(x) {
            console.log("(" + x + ")");
            return 10 * x;
        }

        console.log([t(1), t(2)][0]);
        console.log([t(1), t(2)][1]);
        console.log([t(1), t(2)][2]);

        console.log([...[], t(1), t(2)][0]);
        console.log([...[], t(1), t(2)][1]);
        console.log([...[], t(1), t(2)][2]);

        console.log([t(1), ...[], t(2)][0]);
        console.log([t(1), ...[], t(2)][1]);
        console.log([t(1), ...[], t(2)][2]);

        console.log([t(1), t(2), ...[]][0]);
        console.log([t(1), t(2), ...[]][1]);
        console.log([t(1), t(2), ...[]][2])""".stripMargin.trim,
      expected = """function t(x) {
            console.log("(" + x + ")");
            return 10 * x;
        }

        console.log([ t(1), t(2) ][0]);
        console.log((t(1), t(2)));
        console.log([ t(1), t(2) ][2]);

        console.log([ t(1), t(2) ][0]);
        console.log((t(1), t(2)));
        console.log([ t(1), t(2) ][2]);

        console.log([ t(1), t(2) ][0]);
        console.log((t(1), t(2)));
        console.log([ t(1), t(2) ][2]);

        console.log([ t(1), t(2) ][0]);
        console.log((t(1), t(2)));
        console.log([ t(1), t(2) ][2])""".stripMargin.trim,
      options = AllOff.copy(
        properties = true,
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // array_literal_with_spread_4b
  // =========================================================================
  test("array_literal_with_spread_4b".fail) {
    assertCompresses(
      input = """var nothing = [];
        function t(x) {
            console.log("(" + x + ")");
            return 10 * x;
        }

        console.log([t(1), t(2)][0]);
        console.log([t(1), t(2)][1]);
        console.log([t(1), t(2)][2]);

        console.log([...nothing, t(1), t(2)][0]);
        console.log([...nothing, t(1), t(2)][1]);
        console.log([...nothing, t(1), t(2)][2]);

        console.log([t(1), ...nothing, t(2)][0]);
        console.log([t(1), ...nothing, t(2)][1]);
        console.log([t(1), ...nothing, t(2)][2]);

        console.log([t(1), t(2), ...nothing][0]);
        console.log([t(1), t(2), ...nothing][1]);
        console.log([t(1), t(2), ...nothing][2])""".stripMargin.trim,
      expected = """var nothing = [];
        function t(x) {
            console.log("(" + x + ")");
            return 10 * x;
        }

        console.log([ t(1), t(2) ][0]);
        console.log((t(1), t(2)));
        console.log([ t(1), t(2) ][2]);

        console.log([ ...nothing, t(1), t(2) ][0]);
        console.log([ ...nothing, t(1), t(2) ][1]);
        console.log([ ...nothing, t(1), t(2) ][2]);

        console.log([ t(1), t(2) ][0]);
        console.log([ t(1), ...nothing, t(2) ][1]);
        console.log([ t(1), ...nothing, t(2) ][2]);

        console.log([ t(1), t(2) ][0]);
        console.log((t(1), t(2)));
        console.log([ t(1), t(2), ...nothing ][2])""".stripMargin.trim,
      options = AllOff.copy(
        properties = true,
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // object_literal_method_using_arguments
  // =========================================================================
  test("object_literal_method_using_arguments".fail) {
    assertCompresses(
      input = """console.log(({
            m() {
                return arguments[0];
            }
        }).m("PASS"))""".stripMargin.trim,
      expected = """console.log(({
            m() {
                return arguments[0];
            }
        }).m("PASS"))""".stripMargin.trim,
      options = AllOff.copy(
        arrows = true
      )
    )
  }

  // =========================================================================
  // class_method_using_arguments
  // =========================================================================
  test("class_method_using_arguments".fail) {
    assertCompresses(
      input = """console.log(new class {
            m() {
                return arguments[0];
            }
        }().m("PASS"))""".stripMargin.trim,
      expected = """console.log(new class {
            m() {
                return arguments[0];
            }
        }().m("PASS"))""".stripMargin.trim,
      options = AllOff.copy(
        arrows = true
      )
    )
  }

  // =========================================================================
  // issue_2676
  // =========================================================================
  test("issue_2676".fail) {
    assertCompresses(
      input = """class A {}
        A.a = 42""".stripMargin.trim,
      expected = "(class {}).a = 42",
      options = AllOff.copy(
        reduceVars = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2762
  // =========================================================================
  test("issue_2762".fail) {
    assertCompresses(
      input = """var bar = 1, T = true;
        (function() {
            if (T) {
                const a = function() {
                    var foo = bar;
                    console.log(foo, a.prop, b.prop);
                };
                a.prop = 2;
                const b = { prop: 3 };
                a();
            }
        })()""".stripMargin.trim,
      expected = """var bar = 1, T = true;
        (function() {
            if (T) {
                const o = function() {
                    var p = bar;
                    console.log(p, o.prop, r.prop);
                };
                o.prop = 2;
                const r = { prop: 3 };
                o();
            }
        })()""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // issue_2794_1
  // =========================================================================
  test("issue_2794_1".fail) {
    assertCompresses(
      input = """function foo() {
            for (const a of func(value)) {
                console.log(a);
            }
            function func(va) {
                return doSomething(va);
            }
        }
        function doSomething(x) {
            return [ x, 2 * x, 3 * x ];
        }
        const value = 10;
        foo()""".stripMargin.trim,
      expected = """function foo() {
            for (const a of doSomething(value)) console.log(a);
        }
        function doSomething(x) {
            return [ x, 2 * x, 3 * x ];
        }
        const value = 10;
        foo()""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        evaluate = true,
        inline = InlineLevel.InlineFull,
        passes = 1,
        properties = true,
        reduceFuncs = true,
        reduceVars = true,
        sideEffects = true,
        toplevel = ToplevelConfig(),
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2794_2
  // =========================================================================
  test("issue_2794_2".fail) {
    assertCompresses(
      input = """function foo() {
            for (const a of func(value)) {
                console.log(a);
            }
            function func(va) {
                return doSomething(va);
            }
        }
        function doSomething(x) {
            return [ x, 2 * x, 3 * x ];
        }
        const value = 10;
        foo()""".stripMargin.trim,
      expected = """function foo() {
            for (const o of doSomething(value)) console.log(o);
        }
        function doSomething(o) {
            return [ o, 2 * o, 3 * o ];
        }
        const value = 10;
        foo()""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        evaluate = true,
        inline = InlineLevel.InlineFull,
        passes = 1,
        properties = true,
        reduceFuncs = true,
        reduceVars = true,
        sideEffects = true,
        toplevel = ToplevelConfig(),
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2794_3
  // =========================================================================
  test("issue_2794_3".fail) {
    assertCompresses(
      input = """function foo() {
            for (const a of func(value)) {
                console.log(a);
            }
            function func(va) {
                return doSomething(va);
            }
        }
        function doSomething(x) {
            return [ x, 2 * x, 3 * x ];
        }
        const value = 10;
        foo()""".stripMargin.trim,
      expected = """(function() {
            for (const o of [ 10, 20, 30 ]) console.log(o);
        })()""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        evaluate = true,
        inline = InlineLevel.InlineFull,
        passes = 3,
        properties = true,
        reduceFuncs = true,
        reduceVars = true,
        sideEffects = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // inline_arrow_using_arguments
  // =========================================================================
  test("inline_arrow_using_arguments".fail) {
    assertCompresses(
      input = """(function(){
            ((x) => {
                console.log.apply(console, arguments),
                console.log(x);
            })(4);
        })(3, 2, 1)""".stripMargin.trim,
      expected = """(function(){
            console.log.apply(console, arguments),
            console.log(4);
        })(3, 2, 1)""".stripMargin.trim,
      options = AllOff.copy(
        evaluate = true,
        inline = InlineLevel.InlineSimple,
        reduceFuncs = true,
        reduceVars = true,
        sequencesLimit = 200,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2874_1
  // =========================================================================
  test("issue_2874_1".fail) {
    assertCompresses(
      input = """(function() {
            function foo() {
                let letters = ["A", "B", "C"];
                let result = [2, 1, 0].map(key => bar(letters[key] + key));
                return result;
            }
            function bar(value) {
                return () => console.log(value);
            }
            foo().map(fn => fn());
        })()""".stripMargin.trim,
      expected = """(function () {
            let letters = ["A", "B", "C"];
            return [2, 1, 0].map(key => {
                return value = letters[key] + key, () => console.log(value);
                var value;
            });
        })().map(fn => fn())""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        evaluate = true,
        inline = InlineLevel.InlineFull,
        reduceFuncs = true,
        reduceVars = true,
        sequencesLimit = 200,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2874_2
  // =========================================================================
  test("issue_2874_2".fail) {
    assertCompresses(
      input = """(function() {
            let keys = [];
            function foo() {
                var result = [2, 1, 0].map(value => {
                    keys.push(value);
                    return bar();
                });
                return result;
            }
            function bar() {
                var letters = ["A", "B", "C"], key = keys.shift();
                return () => console.log(letters[key] + key);
            }
            foo().map(fn => fn());
        })()""".stripMargin.trim,
      expected = """(function() {
            let keys = [];
            [ 2, 1, 0 ].map(value => {
                return keys.push(value), letters = [ "A", "B", "C" ], key = keys.shift(), () => console.log(letters[key] + key);
                var letters, key;
            }).map(fn => fn());
        })()""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        evaluate = true,
        inline = InlineLevel.InlineFull,
        reduceFuncs = true,
        reduceVars = true,
        sequencesLimit = 200,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2874_3
  // =========================================================================
  test("issue_2874_3".fail) {
    assertCompresses(
      input = """function f() {
            return x + y;
        }
        let x, y;
        let a = (z) => {
            x = "A";
            y = z;
            console.log(f());
        }
        a(1);
        a(2)""".stripMargin.trim,
      expected = """let x, y;
        let a = z => {
            x = "A",
            y = z,
            console.log(x + y);
        };
        a(1),
        a(2)""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        evaluate = true,
        inline = InlineLevel.InlineFull,
        reduceFuncs = false,
        reduceVars = true,
        sequencesLimit = 200,
        sideEffects = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_3061
  // =========================================================================
  test("issue_3061".fail) {
    assertCompresses(
      input = """console.log(new class extends(function(base) {
            return class extends base {};
        }(Error)){}() instanceof Error)""".stripMargin.trim,
      expected = """console.log(new class extends(function(base) {
            return class extends base {};
        }(Error)){}() instanceof Error)""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        evaluate = true,
        inline = InlineLevel.InlineFull,
        reduceFuncs = false,
        reduceVars = true,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // module_enables_strict_mode
  // =========================================================================
  test("module_enables_strict_mode") {
    assertCompresses(
      input = """if (1) {
            function xyz() {}
        }""".stripMargin.trim,
      expected = """if (1) {
            function xyz() {}
        }""".stripMargin.trim,
      options = AllOff.copy(
        module = true
      )
    )
  }

  // =========================================================================
  // module_mangle_scope
  // =========================================================================
  test("module_mangle_scope".fail) {
    assertCompresses(
      input = "let a = 10",
      expected = "let e = 10",
      options = AllOff
    )
  }

  // =========================================================================
  // module_enabled
  // =========================================================================
  test("module_enabled".fail) {
    assertCompresses(
      input = """let apple = 10, b = 20;
        console.log(apple++, b, apple++);
        export { apple }""".stripMargin.trim,
      expected = """let o = 10;
        console.log(o++, 20, o++);
        export { o as apple }""".stripMargin.trim,
      options = AllOff.copy(
        module = true,
        reduceVars = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_3028
  // =========================================================================
  test("issue_3028".fail) {
    assertCompresses(
      input = """function a(array) {
            var shifted = array.splice(0, 2);
            return [ ...array, ...shifted ];
        }
        console.log(a([ 1, 2, 3 ]).join(" "))""".stripMargin.trim,
      expected = """function a(array) {
            var shifted = array.splice(0, 2);
            return [ ...array, ...shifted ];
        }
        console.log(a([ 1, 2, 3 ]).join(" "))""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        evaluate = true,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_t80
  // =========================================================================
  test("issue_t80".fail) {
    assertCompresses(
      input = """function foo(data = []) {
            var u, v = "unused";
            if (arguments.length == 1) {
                data = [data];
            }
            return data;
        }
        console.log(JSON.stringify([foo(), foo(null), foo(5, 6)]))""".stripMargin.trim,
      expected = """function foo(data = []) {
            if (arguments.length == 1)
                data = [data];
            return data;
        }
        console.log(JSON.stringify([foo(), foo(null), foo(5, 6)]))""".stripMargin.trim,
      options = AllOff.copy(
        unused = true
      )
    )
  }

}
