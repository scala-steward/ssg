/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for ie8.
 * Ported from: terser/test/compress/ie8.js (9 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIe8Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // do_screw_try_catch
  // =========================================================================
  test("do_screw_try_catch".fail) {
    assertCompresses(
      input = """good = function(e){
            return function(error){
                try{
                    e()
                } catch(e) {
                    error(e)
                }
            }
        }""".stripMargin.trim,
      expected = """good = function(n){
            return function(t){
                try{
                    n()
                } catch(n) {
                    t(n)
                }
            }
        }""".stripMargin.trim,
      options = AllOff.copy(
        ie8 = false
      )
    )
  }

  // =========================================================================
  // dont_screw_try_catch
  // =========================================================================
  test("dont_screw_try_catch".fail) {
    assertCompresses(
      input = """bad = function(e){
            return function(error){
                try{
                    e()
                } catch(e) {
                    error(e)
                }
            }
        }""".stripMargin.trim,
      expected = """bad = function(n){
            return function(t){
                try{
                    n()
                } catch(n) {
                    t(n)
                }
            }
        }""".stripMargin.trim,
      options = AllOff.copy(
        ie8 = true
      )
    )
  }

  // =========================================================================
  // do_screw_try_catch_undefined
  // =========================================================================
  test("do_screw_try_catch_undefined".fail) {
    assertCompresses(
      input = """function a(b){
            try {
                throw 'Stuff';
            } catch (undefined) {
                console.log('caught: ' + undefined);
            }
            console.log('undefined is ' + undefined);
            return b === undefined;
        }""".stripMargin.trim,
      expected = """function a(o){
            try{
                throw "Stuff"
            } catch (o) {
                console.log("caught: "+o)
            }
            console.log("undefined is " + void 0);
            return o===void 0
        }""".stripMargin.trim,
      options = AllOff.copy(
        ie8 = false
      )
    )
  }

  // =========================================================================
  // dont_screw_try_catch_undefined
  // =========================================================================
  test("dont_screw_try_catch_undefined".fail) {
    assertCompresses(
      input = """function a(b){
            try {
                throw 'Stuff';
            } catch (undefined) {
                console.log('caught: ' + undefined);
            }
            console.log('undefined is ' + undefined);
            return b === undefined;
        }""".stripMargin.trim,
      expected = """function a(n){
            try{
                throw "Stuff"
            } catch (undefined) {
                console.log("caught: " + undefined)
            }
            console.log("undefined is " + undefined);
            return n === undefined
        }""".stripMargin.trim,
      options = AllOff.copy(
        ie8 = true
      )
    )
  }

  // =========================================================================
  // reduce_vars
  // =========================================================================
  test("reduce_vars".fail) {
    assertCompresses(
      input = """function f() {
            var a;
            try {
                x();
            } catch (a) {
                y();
            }
            alert(a);
        }""".stripMargin.trim,
      expected = """function f() {
            var t;
            try {
                x();
            } catch (t) {
                y();
            }
            alert(t);
        }""".stripMargin.trim,
      options = AllOff.copy(
        evaluate = true,
        ie8 = true,
        reduceFuncs = true,
        reduceVars = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2120_1
  // =========================================================================
  test("issue_2120_1".fail) {
    assertCompresses(
      input = """"aaaaaaaa";
        var a = 1, b = "FAIL";
        try {
            throw 1;
        } catch (c) {
            try {
                throw 0;
            } catch (a) {
                if (c) b = "PASS";
            }
        }
        console.log(b)""".stripMargin.trim,
      expected = """"aaaaaaaa";
        var a = 1, b = "FAIL";
        try {
            throw 1;
        } catch (t) {
            try {
                throw 0;
            } catch (a) {
                if (t) b = "PASS";
            }
        }
        console.log(b)""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // issue_2120_2
  // =========================================================================
  test("issue_2120_2") {
    assertCompresses(
      input = """"aaaaaaaa";
        var a = 1, b = "FAIL";
        try {
            throw 1;
        } catch (c) {
            try {
                throw 0;
            } catch (a) {
                if (c) b = "PASS";
            }
        }
        console.log(b)""".stripMargin.trim,
      expected = """"aaaaaaaa";
        var a = 1, b = "FAIL";
        try {
            throw 1;
        } catch (c) {
            try {
                throw 0;
            } catch (a) {
                if (c) b = "PASS";
            }
        }
        console.log(b)""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // issue_2254_1
  // =========================================================================
  test("issue_2254_1".fail) {
    assertCompresses(
      input = """"eeeeee";
        try {
            console.log(f("PASS"));
        } catch (e) {}
        function f(s) {
            try {
                throw "FAIL";
            } catch (e) {
                return s;
            }
        }""".stripMargin.trim,
      expected = """"eeeeee";
        try {
            console.log(f("PASS"));
        } catch (e) {}
        function f(e) {
            try {
                throw "FAIL";
            } catch (t) {
                return e;
            }
        }""".stripMargin.trim,
      options = AllOff
    )
  }

  // =========================================================================
  // issue_2254_2
  // =========================================================================
  test("issue_2254_2".fail) {
    assertCompresses(
      input = """"eeeeee";
        try {
            console.log(f("PASS"));
        } catch (e) {}
        function f(s) {
            try {
                throw "FAIL";
            } catch (e) {
                return s;
            }
        }""".stripMargin.trim,
      expected = """"eeeeee";
        try {
            console.log(f("PASS"));
        } catch (e) {}
        function f(t) {
            try {
                throw "FAIL";
            } catch (e) {
                return t;
            }
        }""".stripMargin.trim,
      options = AllOff
    )
  }

}
