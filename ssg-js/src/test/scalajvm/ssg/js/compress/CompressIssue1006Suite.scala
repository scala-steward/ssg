/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-1006.
 * Ported from: terser/test/compress/issue-1006.js (1 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue1006Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // issue_1006
  // =========================================================================
  test("issue_1006".fail) {
    assertCompresses(
      input = """function func(foo) {
            function bar() {
                return;
            }
            let baz = foo();
            if (baz !== undefined) {
                let qux = bar(baz.qux);
            }
        }""".stripMargin.trim,
      expected = """function func(foo) {
            let baz = foo();
            if (void 0 !== baz) {
                baz.qux;
            }
        }""".stripMargin.trim,
      options = AllOff
    )
  }

}
