/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-12.
 * Ported from: terser/test/compress/issue-12.js (3 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue12Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // keep_name_of_getter
  // =========================================================================
  test("keep_name_of_getter".fail) {
    assertCompresses(
      input = "a = { get foo () {} }",
      expected = "a = { get foo () {} }",
      options = AllOff.copy(
        unused = true
      )
    )
  }

  // =========================================================================
  // keep_name_of_setter
  // =========================================================================
  test("keep_name_of_setter".fail) {
    assertCompresses(
      input = "a = { set foo () {} }",
      expected = "a = { set foo () {} }",
      options = AllOff.copy(
        unused = true
      )
    )
  }

  // =========================================================================
  // setter_with_operator_keys
  // =========================================================================
  test("setter_with_operator_keys".fail) {
    assertCompresses(
      input = """var tokenCodes  = {
            get instanceof(){
                return test0;
            },
            set instanceof(value){
                test0 = value;
            },
            set typeof(value){
                test1 = value;
            },
            get typeof(){
                return test1;
            },
            set else(value){
                test2 = value;
            },
            get else(){
                return test2;
            }
        }""".stripMargin.trim,
      expected = """var tokenCodes  = {
            get instanceof(){
                return test0;
            },
            set instanceof(value){
                test0 = value;
            },
            set typeof(value){
                test1 = value;
            },
            get typeof(){
                return test1;
            },
            set else(value){
                test2 = value;
            },
            get else(){
                return test2;
            }
        }""".stripMargin.trim,
      options = AllOff
    )
  }

}
