/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-1429.
 * Ported from: terser/test/compress/issue-1429.js (4 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }
import ssg.js.compress.ToplevelConfig

final class CompressIssue1429Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // variant_1
  // =========================================================================
  test("variant_1".fail) {
    assertCompresses(
      input = """const A = 1, B = 0, C = 2, D = 3;

        export function f() {
            return A * (B + C + 2 * D);
        }""".stripMargin.trim,
      expected = """export function f() {
            return 8;
        }""".stripMargin.trim,
      options = AllOff.copy(
        evaluate = true,
        reduceVars = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // variant_2
  // =========================================================================
  test("variant_2".fail) {
    assertCompresses(
      input = """const A = 1, B = 0, C = 2, D = 3;

        export function f() {
            return A * (B + C + 2 * D);
        }

        export default function () { return f; }""".stripMargin.trim,
      expected = """export function f() {
            return 8;
        }

        export default function () { return f; }""".stripMargin.trim,
      options = AllOff.copy(
        evaluate = true,
        reduceVars = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // variant_3
  // =========================================================================
  test("variant_3".fail) {
    assertCompresses(
      input = """const A = 1, B = 0, C = 2, D = 3;

        export function f() {
            return A * (B + C + 2 * D);
        }

        export function g() { return f; }""".stripMargin.trim,
      expected = """export function f() {
            return 8;
        }

        export function g() { return f; }""".stripMargin.trim,
      options = AllOff.copy(
        evaluate = true,
        reduceVars = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // variant_4
  // =========================================================================
  test("variant_4".fail) {
    assertCompresses(
      input = """const A = 1, B = 0, C = 2, D = 3;

        export function f() {
            return A * (B + C + 2 * D);
        }

        export default function g() { return f; }""".stripMargin.trim,
      expected = """export function f() {
            return 8;
        }

        export default function g() { return f; }""".stripMargin.trim,
      options = AllOff.copy(
        evaluate = true,
        reduceVars = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

}
