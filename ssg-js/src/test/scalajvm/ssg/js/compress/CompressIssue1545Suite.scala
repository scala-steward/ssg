/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-1545.
 * Ported from: terser/test/compress/issue-1545.js (1 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }
import ssg.js.compress.ToplevelConfig

final class CompressIssue1545Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // issue_1545
  // =========================================================================
  test("issue_1545".fail) {
    assertCompresses(
      input = """console.assert(true, "This should be removed");
        console.assert(1, "This should be removed too");
        console.assert(false, "This should remain");
        console.assert(0, "This should remain as well")""".stripMargin.trim,
      expected = """console.assert(false, "This should remain");
        console.assert(0, "This should remain as well")""".stripMargin.trim,
      options = AllOff.copy(
        evaluate = true,
        sideEffects = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

}
