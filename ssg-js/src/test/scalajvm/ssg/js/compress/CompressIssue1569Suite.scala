/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-1569.
 * Ported from: terser/test/compress/issue-1569.js (1 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue1569Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // inner_reference
  // =========================================================================
  test("inner_reference".fail) {
    assertCompresses(
      input = """!function f(a) {
            return a && f(a - 1) + a;
        }(42);
        !function g(a) {
            return a;
        }(42)""".stripMargin.trim,
      expected = """!function f(a) {
            return a && f(a - 1) + a;
        }(42);
        !void 0""".stripMargin.trim,
      options = AllOff.copy(
        sideEffects = true
      )
    )
  }

}
