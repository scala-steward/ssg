/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-1639.
 * Ported from: terser/test/compress/issue-1639.js (3 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue1639Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // issue_1639_1
  // =========================================================================
  test("issue_1639_1".fail) {
    assertCompresses(
      input = """var a = 100, b = 10;

        var L1 = 5;
        while (--L1 > 0) {
            if ((--b), false) {
                if (b) {
                    var ignore = 0;
                }
            }
        }

        console.log(a, b)""".stripMargin.trim,
      expected = """for (var a = 100, b = 10, L1 = 5; --L1 > 0;)
            if (--b, 0) var ignore = 0;
        console.log(a, b)""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = true,
        conditionals = true,
        evaluate = true,
        joinVars = true,
        loops = true,
        sequencesLimit = 200,
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // issue_1639_2
  // =========================================================================
  test("issue_1639_2".fail) {
    assertCompresses(
      input = """var a = 100, b = 10;

        function f19() {
            if (++a, false)
                if (a)
                    if (++a);
        }
        f19();

        console.log(a, b)""".stripMargin.trim,
      expected = """var a = 100, b = 10;
        function f19() {
            ++a, 0;
        }
        f19(),
        console.log(a, b)""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = true,
        conditionals = true,
        evaluate = true,
        joinVars = true,
        sequencesLimit = 200,
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // issue_1639_3
  // =========================================================================
  test("issue_1639_3".fail) {
    assertCompresses(
      input = """var a = 100, b = 10;
        a++ && false && a ? 0 : 0;
        console.log(a, b)""".stripMargin.trim,
      expected = """var a = 100, b = 10;
        a++,
        console.log(a, b)""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        collapseVars = true,
        conditionals = true,
        evaluate = true,
        sequencesLimit = 200,
        sideEffects = true
      )
    )
  }

}
