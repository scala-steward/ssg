/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-637.
 * Ported from: terser/test/compress/issue-637.js (1 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue637Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // wrongly_optimized
  // =========================================================================
  test("wrongly_optimized".fail) {
    assertCompresses(
      input = """function func() {
            foo();
        }
        if (func() || true) {
            bar();
        }""".stripMargin.trim,
      expected = """function func() {
            foo();
        }
        // TODO: optimize to `func(), bar()`
        (func(), 1) && bar()""".stripMargin.trim,
      options = AllOff.copy(
        booleans = true,
        conditionals = true,
        evaluate = true
      )
    )
  }

}
