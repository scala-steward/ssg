/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-913.
 * Ported from: terser/test/compress/issue-913.js (1 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssue913Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // keep_var_for_in
  // =========================================================================
  test("keep_var_for_in") {
    assertCompresses(
      input = """(function(obj){
            var foo = 5;
            for (var i in obj)
                return foo;
        })()""".stripMargin.trim,
      expected = """(function(obj){
            var i, foo = 5;
            for (i in obj)
                return foo;
        })()""".stripMargin.trim,
      options = AllOff.copy(
        hoistVars = true,
        unused = true
      )
    )
  }

}
