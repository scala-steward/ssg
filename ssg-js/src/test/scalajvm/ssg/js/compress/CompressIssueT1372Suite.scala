/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for issue-t1372.
 * Ported from: terser/test/compress/issue-t1372.js (1 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressIssueT1372Suite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // issue_t1372_maintain_this_binding
  // =========================================================================
  test("issue_t1372_maintain_this_binding".fail) {
    assertCompresses(
      input = """(function(o) {
            console.log((0, o.f)("PASS"), (0, o?.f)("PASS"));
        })({
            a: "FAIL",
            f(b) {
                return this.a || b;
            },
        })""".stripMargin.trim,
      expected = """(function(o) {
            console.log((0, o.f)("PASS"), (0, o?.f)("PASS"));
        })({
            a: "FAIL",
            f(b) {
                return this.a || b;
            },
        })""".stripMargin.trim,
      options = AllOff.copy(
        sideEffects = true
      )
    )
  }

}
