/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for pure_getters.
 * Ported from: terser/test/compress/pure_getters.js (41 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }
import ssg.js.compress.ToplevelConfig

final class CompressPureGettersSuite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // strict
  // =========================================================================
  test("strict") {
    assertCompresses(
      input = """var a, b = null, c = {};
        a.prop;
        b.prop;
        c.prop;
        d.prop;
        null.prop;
        (void 0).prop;
        undefined.prop""".stripMargin.trim,
      expected = """var a, b = null, c = {};
        a.prop;
        b.prop;
        c.prop;
        d.prop;
        null.prop;
        (void 0).prop;
        (void 0).prop""".stripMargin.trim,
      options = AllOff.copy(
        pureGetters = "strict",
        reduceFuncs = false,
        reduceVars = false,
        sideEffects = true,
        toplevel = ToplevelConfig(funcs = true, vars = true)
      )
    )
  }

  // =========================================================================
  // strict_reduce_vars
  // =========================================================================
  test("strict_reduce_vars".fail) {
    assertCompresses(
      input = """var a, b = null, c = {};
        a.prop;
        b.prop;
        c.prop;
        d.prop;
        null.prop;
        (void 0).prop;
        undefined.prop""".stripMargin.trim,
      expected = """var a, b = null, c = {};
        a.prop;
        b.prop;
        d.prop;
        null.prop;
        (void 0).prop;
        (void 0).prop""".stripMargin.trim,
      options = AllOff.copy(
        pureGetters = "strict",
        reduceFuncs = true,
        reduceVars = true,
        sideEffects = true,
        toplevel = ToplevelConfig(funcs = true, vars = true)
      )
    )
  }

  // =========================================================================
  // unsafe
  // =========================================================================
  test("unsafe".fail) {
    assertCompresses(
      input = """var a, b = null, c = {};
        a.prop;
        b.prop;
        c.prop;
        d.prop;
        null.prop;
        (void 0).prop;
        undefined.prop""".stripMargin.trim,
      expected = """var a, b = null, c = {};
        d;
        null.prop;
        (void 0).prop;
        (void 0).prop""".stripMargin.trim,
      options = AllOff.copy(
        pureGetters = "strict",
        reduceFuncs = false,
        reduceVars = false,
        sideEffects = true,
        toplevel = ToplevelConfig(funcs = true, vars = true)
      )
    )
  }

  // =========================================================================
  // unsafe_reduce_vars
  // =========================================================================
  test("unsafe_reduce_vars".fail) {
    assertCompresses(
      input = """var a, b = null, c = {};
        a.prop;
        b.prop;
        c.prop;
        d.prop;
        null.prop;
        (void 0).prop;
        undefined.prop""".stripMargin.trim,
      expected = """var a, b = null, c = {};
        d;
        null.prop;
        (void 0).prop;
        (void 0).prop""".stripMargin.trim,
      options = AllOff.copy(
        pureGetters = "strict",
        reduceFuncs = true,
        reduceVars = true,
        sideEffects = true,
        toplevel = ToplevelConfig(funcs = true, vars = true)
      )
    )
  }

  // =========================================================================
  // chained
  // =========================================================================
  test("chained") {
    assertCompresses(
      input = "a.b.c",
      expected = "a.b.c",
      options = AllOff.copy(
        pureGetters = "strict",
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // impure_getter_1
  // =========================================================================
  test("impure_getter_1".fail) {
    assertCompresses(
      input = """({
            get a() {
                console.log(1);
            },
            b: 1
        }).a;
        ({
            get a() {
                console.log(1);
            },
            b: 1
        }).b""".stripMargin.trim,
      expected = """({
            get a() {
                console.log(1);
            },
            b: 1
        }).a;
        ({
            get a() {
                console.log(1);
            },
            b: 1
        }).b""".stripMargin.trim,
      options = AllOff.copy(
        pureGetters = "strict",
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // impure_getter_2
  // =========================================================================
  test("impure_getter_2".fail) {
    assertCompresses(
      input = """// will produce incorrect output because getter is not pure
        ({
            get a() {
                console.log(1);
            },
            b: 1
        }).a;
        ({
            get a() {
                console.log(1);
            },
            b: 1
        }).b""".stripMargin.trim,
      expected = "",
      options = AllOff.copy(
        pureGetters = "strict",
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // issue_2110_1
  // =========================================================================
  test("issue_2110_1".fail) {
    assertCompresses(
      input = """function f() {
            function f() {}
            function g() {
                return this;
            }
            f.g = g;
            return f.g();
        }
        console.log(typeof f())""".stripMargin.trim,
      expected = """function f() {
            function f() {}
            return f.g = function() {
                return this;
            }, f.g();
        }
        console.log(typeof f())""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        pureGetters = "strict",
        reduceFuncs = true,
        reduceVars = true,
        sequencesLimit = 200,
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2110_2
  // =========================================================================
  test("issue_2110_2".fail) {
    assertCompresses(
      input = """function f() {
            function f() {}
            function g() {
                return this;
            }
            f.g = g;
            return f.g();
        }
        console.log(typeof f())""".stripMargin.trim,
      expected = """function f() {
            function f() {}
            f.g = function() {
                return this;
            };
            return f.g();
        }
        console.log(typeof f())""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        pureGetters = "strict",
        reduceFuncs = true,
        reduceVars = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // set_immutable_1
  // =========================================================================
  test("set_immutable_1".fail) {
    assertCompresses(
      input = """var a = 1;
        a.foo += "";
        if (a.foo) console.log("FAIL");
        else console.log("PASS")""".stripMargin.trim,
      expected = """1..foo += "";
        if (1..foo) console.log("FAIL");
        else console.log("PASS")""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        evaluate = true,
        pureGetters = "strict",
        reduceFuncs = true,
        reduceVars = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // set_immutable_2
  // =========================================================================
  test("set_immutable_2".fail) {
    assertCompresses(
      input = """var a = 1;
        a.foo += "";
        if (a.foo) console.log("FAIL");
        else console.log("PASS")""".stripMargin.trim,
      expected = """var a = 1;
        a.foo += "", a.foo ? console.log("FAIL") : console.log("PASS")""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        conditionals = true,
        pureGetters = "strict",
        reduceFuncs = true,
        reduceVars = true,
        sequencesLimit = 200,
        sideEffects = true,
        toplevel = ToplevelConfig(funcs = true, vars = true)
      )
    )
  }

  // =========================================================================
  // set_immutable_3
  // =========================================================================
  test("set_immutable_3".fail) {
    assertCompresses(
      input = """"use strict";
        var a = 1;
        a.foo += "";
        if (a.foo) console.log("FAIL");
        else console.log("PASS")""".stripMargin.trim,
      expected = """"use strict";
        1..foo += "";
        if (1..foo) console.log("FAIL");
        else console.log("PASS")""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        evaluate = true,
        pureGetters = "strict",
        reduceFuncs = true,
        reduceVars = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // set_immutable_4
  // =========================================================================
  test("set_immutable_4".fail) {
    assertCompresses(
      input = """"use strict";
        var a = 1;
        a.foo += "";
        if (a.foo) console.log("FAIL");
        else console.log("PASS")""".stripMargin.trim,
      expected = """"use strict";
        var a = 1;
        a.foo += "", a.foo ? console.log("FAIL") : console.log("PASS")""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        conditionals = true,
        pureGetters = "strict",
        reduceFuncs = true,
        reduceVars = true,
        sequencesLimit = 200,
        sideEffects = true,
        toplevel = ToplevelConfig(funcs = true, vars = true)
      )
    )
  }

  // =========================================================================
  // set_immutable_5
  // =========================================================================
  test("set_immutable_5".fail) {
    assertCompresses(
      input = """"use strict";
        var a = 1;
        a.foo += "";
        if (a.foo) console.log("FAIL");
        else console.log("PASS")""".stripMargin.trim,
      expected = """"use strict";
        1..foo += "";
        1..foo ? console.log("FAIL") : console.log("PASS")""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        conditionals = true,
        evaluate = true,
        pureGetters = "strict",
        reduceFuncs = true,
        reduceVars = true,
        sideEffects = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // set_immutable_6
  // =========================================================================
  test("set_immutable_6".fail) {
    assertCompresses(
      input = """var a = 1;
        a.foo += "";
        if (a.foo) console.log("FAIL");
        else console.log("PASS")""".stripMargin.trim,
      expected = "1..foo ? console.log(\"FAIL\") : console.log(\"PASS\")",
      options = AllOff.copy(
        collapseVars = true,
        conditionals = true,
        evaluate = true,
        pureGetters = "strict",
        reduceFuncs = true,
        reduceVars = true,
        sideEffects = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // set_mutable_1
  // =========================================================================
  test("set_mutable_1".fail) {
    assertCompresses(
      input = """!function a() {
            a.foo += "";
            if (a.foo) console.log("PASS");
            else console.log("FAIL");
        }()""".stripMargin.trim,
      expected = """!function a() {
            if (a.foo += "") console.log("PASS");
            else console.log("FAIL");
        }()""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        evaluate = true,
        pureGetters = "strict",
        reduceFuncs = true,
        reduceVars = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // set_mutable_2
  // =========================================================================
  test("set_mutable_2".fail) {
    assertCompresses(
      input = """!function a() {
            a.foo += "";
            if (a.foo) console.log("PASS");
            else console.log("FAIL");
        }()""".stripMargin.trim,
      expected = """!function a() {
            (a.foo += "") ? console.log("PASS") : console.log("FAIL");
        }()""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        conditionals = true,
        pureGetters = "strict",
        reduceFuncs = true,
        reduceVars = true,
        sequencesLimit = 200,
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // issue_2265_1
  // =========================================================================
  test("issue_2265_1".fail) {
    assertCompresses(
      input = """({ ...{} }).p;
        ({ ...g }).p""".stripMargin.trim,
      expected = "({ ...g }).p",
      options = AllOff.copy(
        pureGetters = "strict",
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // issue_2265_2
  // =========================================================================
  test("issue_2265_2".fail) {
    assertCompresses(
      input = """var a = {
            get b() {
                throw 0;
            }
        };
        ({...a}).b""".stripMargin.trim,
      expected = """var a = {
            get b() {
                throw 0;
            }
        };
        ({...a}).b""".stripMargin.trim,
      options = AllOff.copy(
        pureGetters = "strict",
        reduceFuncs = true,
        reduceVars = true,
        sideEffects = true,
        toplevel = ToplevelConfig(funcs = true, vars = true)
      )
    )
  }

  // =========================================================================
  // issue_2265_3
  // =========================================================================
  test("issue_2265_3".fail) {
    assertCompresses(
      input = """var a = {
            set b() {
                throw 0;
            }
        };
        ({...a}).b""".stripMargin.trim,
      expected = "",
      options = AllOff.copy(
        pureGetters = "strict",
        reduceFuncs = true,
        reduceVars = true,
        sideEffects = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2265_4
  // =========================================================================
  test("issue_2265_4".fail) {
    assertCompresses(
      input = """var a = { b: 1 };
        ({...a}).b""".stripMargin.trim,
      expected = "",
      options = AllOff.copy(
        pureGetters = "strict",
        reduceFuncs = true,
        reduceVars = true,
        sideEffects = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2313_1
  // =========================================================================
  test("issue_2313_1".fail) {
    assertCompresses(
      input = """function x() {
            console.log(1);
            return {
                y: function() {
                    console.log(2);
                    return {
                        z: 0
                    };
                }
            };
        }
        x().y().z++;
        if (x().y().z) {
            console.log(3);
        }""".stripMargin.trim,
      expected = """function x() {
            return console.log(1), {
                y: function() {
                    return console.log(2), {
                        z: 0
                    };
                }
            };
        }
        x().y().z++,
        x().y().z && console.log(3)""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        conditionals = true,
        pureGetters = "strict",
        sequencesLimit = 200,
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // issue_2313_2
  // =========================================================================
  test("issue_2313_2".fail) {
    assertCompresses(
      input = """function x() {
            console.log(1);
            return {
                y: function() {
                    console.log(2);
                    return {
                        z: 0
                    };
                }
            };
        }
        x().y().z++;
        if (x().y().z) {
            console.log(3);
        }""".stripMargin.trim,
      expected = """function x() {
            return console.log(1), {
                y: function() {
                    return console.log(2), {
                        z: 0
                    };
                }
            };
        }
        x().y().z++,
        x().y().z && console.log(3)""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        conditionals = true,
        pureGetters = "strict",
        sequencesLimit = 200,
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // issue_2313_3
  // =========================================================================
  test("issue_2313_3") {
    assertCompresses(
      input = """function x() {
            console.log(1);
            return {
                y: function() {
                    console.log(2);
                    return {
                        z: 0
                    };
                }
            };
        }
        x().y().z++;
        if (x().y().z) {
            console.log(3);
        }""".stripMargin.trim,
      expected = """function x() {
            console.log(1);
            return {
                y: function() {
                    console.log(2);
                    return {
                        z: 0
                    };
                }
            };
        }
        x().y().z++;
        x().y().z && console.log(3)""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        conditionals = true,
        pureGetters = "strict"
      )
    )
  }

  // =========================================================================
  // issue_2313_4
  // =========================================================================
  test("issue_2313_4") {
    assertCompresses(
      input = """function x() {
            console.log(1);
            return {
                y: function() {
                    console.log(2);
                    return {
                        z: 0
                    };
                }
            };
        }
        x().y().z++;
        if (x().y().z) {
            console.log(3);
        }""".stripMargin.trim,
      expected = """function x() {
            console.log(1);
            return {
                y: function() {
                    console.log(2);
                    return {
                        z: 0
                    };
                }
            };
        }
        x().y().z++;
        x().y().z && console.log(3)""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        conditionals = true,
        pureGetters = "strict"
      )
    )
  }

  // =========================================================================
  // issue_2313_5
  // =========================================================================
  test("issue_2313_5") {
    assertCompresses(
      input = """x().y++;
        x().y""".stripMargin.trim,
      expected = """x().y++;
        x().y""".stripMargin.trim,
      options = AllOff.copy(
        pureGetters = "strict",
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // issue_2313_6
  // =========================================================================
  test("issue_2313_6".fail) {
    assertCompresses(
      input = """x().y++;
        x().y""".stripMargin.trim,
      expected = """x().y++;
        x()""".stripMargin.trim,
      options = AllOff.copy(
        pureGetters = "strict",
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // issue_2313_7
  // =========================================================================
  test("issue_2313_7".fail) {
    assertCompresses(
      input = """var a = 0, b = 0;
        class foo {
            get c() {
                a++;
                return 42;
            }
            set c(c) {
                b++;
            }
        }
        class bar extends foo {
            d() {
                super.c++;
                if (super.c) console.log(a, b);
            }
        }
        new bar().d()""".stripMargin.trim,
      expected = """var a = 0, b = 0;
        class foo {
            get c() {
                a++;
                return 42;
            }
            set c(c) {
                b++;
            }
        }
        class bar extends foo {
            d() {
                super.c++;
                super.c && console.log(a, b);
            }
        }
        new bar().d()""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        conditionals = true,
        pureGetters = "strict"
      )
    )
  }

  // =========================================================================
  // issue_2678
  // =========================================================================
  test("issue_2678".fail) {
    assertCompresses(
      input = """var a = 1, c = "FAIL";
        (function f() {
            (a-- && f()).p;
            return {
                get p() {
                    c = "PASS";
                }
            };
        })();
        console.log(c)""".stripMargin.trim,
      expected = """var a = 1, c = "FAIL";
        (function f() {
            (a-- && f()).p;
            return {
                get p() {
                    c = "PASS";
                }
            };
        })();
        console.log(c)""".stripMargin.trim,
      options = AllOff.copy(
        pureGetters = "strict",
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // issue_2838
  // =========================================================================
  test("issue_2838".fail) {
    assertCompresses(
      input = """function f(a, b) {
            (a || b).c = "PASS";
            (function() {
                return f(a, b);
            }).prototype.foo = "bar";
        }
        var o = {};
        f(null, o);
        console.log(o.c)""".stripMargin.trim,
      expected = """function f(a, b) {
            (a || b).c = "PASS";
        }
        var o = {};
        f(null, o);
        console.log(o.c)""".stripMargin.trim,
      options = AllOff.copy(
        pureGetters = "strict",
        sideEffects = true
      )
    )
  }

  // =========================================================================
  // issue_2938_1
  // =========================================================================
  test("issue_2938_1") {
    assertCompresses(
      input = """function f(a) {
            a.b = "PASS";
        }
        var o = {};
        f(o);
        console.log(o.b)""".stripMargin.trim,
      expected = """function f(a) {
            a.b = "PASS";
        }
        var o = {};
        f(o);
        console.log(o.b)""".stripMargin.trim,
      options = AllOff.copy(
        pureGetters = "strict",
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2938_2
  // =========================================================================
  test("issue_2938_2") {
    assertCompresses(
      input = """var Parser = function Parser() {};
        var p = Parser.prototype;
        p.initialContext = function initialContext() {
            console.log("PASS");
        };
        p.braceIsBlock = function() {};
        (new Parser).initialContext()""".stripMargin.trim,
      expected = """var Parser = function() {};
        var p = Parser.prototype;
        p.initialContext = function() {
            console.log("PASS");
        };
        p.braceIsBlock = function() {};
        (new Parser).initialContext()""".stripMargin.trim,
      options = AllOff.copy(
        pureGetters = "strict",
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2938_3
  // =========================================================================
  test("issue_2938_3".fail) {
    assertCompresses(
      input = """function f(a) {
            var unused = a.a;
            a.b = "PASS";
            a.c;
        }
        var o = {};
        o.d;
        f(o);
        console.log(o.b)""".stripMargin.trim,
      expected = """function f(a) {
            a.b = "PASS";
        }
        var o = {};
        f(o);
        console.log(o.b)""".stripMargin.trim,
      options = AllOff.copy(
        pureGetters = "strict",
        sideEffects = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // issue_2938_4
  // =========================================================================
  test("issue_2938_4".fail) {
    assertCompresses(
      input = """var Parser = function Parser() {};
        var p = Parser.prototype;
        var unused = p.x;
        p.initialContext = function initialContext() {
            p.y;
            console.log("PASS");
        };
        p.braceIsBlock = function() {};
        (new Parser).initialContext()""".stripMargin.trim,
      expected = """var Parser = function() {};
        var p = Parser.prototype;
        p.initialContext = function() {
            console.log("PASS");
        };
        p.braceIsBlock = function() {};
        (new Parser).initialContext()""".stripMargin.trim,
      options = AllOff.copy(
        pureGetters = "strict",
        sideEffects = true,
        toplevel = ToplevelConfig(funcs = true, vars = true),
        unused = true
      )
    )
  }

  // =========================================================================
  // collapse_vars_1_true
  // =========================================================================
  test("collapse_vars_1_true".fail) {
    assertCompresses(
      input = """function f(a, b) {
            for (;;) {
                var c = a.g();
                var d = b.p;
                if (c || d) break;
            }
        }""".stripMargin.trim,
      expected = """function f(a, b) {
            for (;;) {
                if (a.g() || b.p) break;
            }
        }""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        pureGetters = "strict",
        unused = true
      )
    )
  }

  // =========================================================================
  // collapse_vars_1_false
  // =========================================================================
  test("collapse_vars_1_false") {
    assertCompresses(
      input = """function f(a, b) {
            for (;;) {
                var c = a.g();
                var d = b.p;
                if (c || d) break;
            }
        }""".stripMargin.trim,
      expected = """function f(a, b) {
            for (;;) {
                var c = a.g();
                var d = b.p;
                if (c || d) break;
            }
        }""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        unused = true
      )
    )
  }

  // =========================================================================
  // collapse_vars_1_strict
  // =========================================================================
  test("collapse_vars_1_strict") {
    assertCompresses(
      input = """function f(a, b) {
            for (;;) {
                var c = a.g();
                var d = b.p;
                if (c || d) break;
            }
        }""".stripMargin.trim,
      expected = """function f(a, b) {
            for (;;) {
                var c = a.g();
                var d = b.p;
                if (c || d) break;
            }
        }""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        pureGetters = "strict",
        unused = true
      )
    )
  }

  // =========================================================================
  // collapse_vars_2_true
  // =========================================================================
  test("collapse_vars_2_true".fail) {
    assertCompresses(
      input = """function f() {
            function g() {}
            g.a = function() {};
            g.b = g.a;
            return g;
        }""".stripMargin.trim,
      expected = """function f() {
            function g() {}
            g.b = g.a = function() {};
            return g;
        }""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        pureGetters = "strict",
        reduceVars = true
      )
    )
  }

  // =========================================================================
  // collapse_vars_2_false
  // =========================================================================
  test("collapse_vars_2_false") {
    assertCompresses(
      input = """function f() {
            function g() {}
            g.a = function() {};
            g.b = g.a;
            return g;
        }""".stripMargin.trim,
      expected = """function f() {
            function g() {}
            g.a = function() {};
            g.b = g.a;
            return g;
        }""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        reduceVars = true
      )
    )
  }

  // =========================================================================
  // collapse_vars_2_strict
  // =========================================================================
  test("collapse_vars_2_strict") {
    assertCompresses(
      input = """function f() {
            function g() {}
            g.a = function() {};
            g.b = g.a;
            return g;
        }""".stripMargin.trim,
      expected = """function f() {
            function g() {}
            g.a = function() {};
            g.b = g.a;
            return g;
        }""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        pureGetters = "strict",
        reduceVars = true
      )
    )
  }

  // =========================================================================
  // collapse_rhs_lhs
  // =========================================================================
  test("collapse_rhs_lhs") {
    assertCompresses(
      input = """function f(a, b) {
            a.b = b, b += 2;
            console.log(a.b, b);
        }
        f({}, 1)""".stripMargin.trim,
      expected = """function f(a, b) {
            a.b = b, b += 2;
            console.log(a.b, b);
        }
        f({}, 1)""".stripMargin.trim,
      options = AllOff.copy(
        collapseVars = true,
        pureGetters = "strict"
      )
    )
  }

}
