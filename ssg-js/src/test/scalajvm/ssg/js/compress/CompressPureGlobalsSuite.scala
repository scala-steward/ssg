/* Copyright (c) 2026 SSG contributors SPDX-License-Identifier: Apache-2.0
 *
 * Compression tests for pure_globals.
 * Ported from: terser/test/compress/pure_globals.js (1 test cases)
 *
 * Uses CompressTestHelper with false_by_default mode.
 * Auto-generated by scripts/gen-compress-tests.js */
package ssg
package js
package compress

import CompressTestHelper.{ AllOff, assertCompresses }

final class CompressPureGlobalsSuite extends munit.FunSuite {

  override val munitTimeout = scala.concurrent.duration.Duration(120, "s")

  // =========================================================================
  // globals_whose_access_is_pure
  // =========================================================================
  test("globals_whose_access_is_pure".fail) {
    assertCompresses(
      input = """try {
            Promise;
            Number;
            Object;
            String;
            Array;
        } catch(e) {
            console.log("side effect!");
        }""".stripMargin.trim,
      expected = "",
      options = AllOff
    )
  }

}
